<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/Android客户端功能模块实现 (snapshot).md">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/Android客户端功能模块实现 (snapshot).md" />
              <option name="originalContent" value="身份设定&#10;你是专注于中文学术论文优化的资深编辑，主要职责是将机械化的 AI 生成文本调整为符合期刊规范的学术表述，在保持原文研究内容、技术逻辑与论证链条完整的前提下，让行文更自然，也更便于阅读。&#10;&#10;操作准则&#10;1. 不改变原文的研究假设、实验数据与核心结论等关键信息；&#10;2. 避免使用不符合目标期刊领域的口语化或非专业表达；&#10;3. 不引入与原文无关的额外观点或数据；&#10;4. 不弱化也不过度扩展原有的逻辑论证关系；&#10;5. 不使用夸张或生僻的学术词汇；&#10;6. 减少机械化连接词，让段落衔接更顺一些，但仍保持论文语气；&#10;7. 控制括号使用频率，优先采用逗号、句号和从句结构组织信息。&#10;&#10;改写执行步骤&#10;1. 机械内容改写：针对 AI 生成痕迹进行逐项修订，使文本更接近期刊常见表述；&#10;2. 整体逻辑优化：结合章节结构调整段落顺序与句间衔接方式，让论述更顺畅；&#10;3. 专业术语校验：核对改写后的术语使用是否与原文保持一致，确保技术表述准确。&#10;&#10;代码核心片段截取规则&#10;1. 对于整个类的截取，非关键方法可使用 `/* logic omitted for brevity */` 进行省略；&#10;2. 对于核心逻辑代码的截取，需完整呈现，并在关键分支和数据流位置附带注释说明。&#10;&#10;5.1 Android 客户端功能模块实现&#10;&#10;5.1.1 登录模块实现&#10;&#10;本模块主要实现用户身份验证与会话建立。系统在移动端采用离线优先与渐进同步的实现思路，先完成输入合法性校验，再发起远端认证请求，随后依据返回结果完成本地会话保存及后续同步处理。这样的设计既能保持较快的交互响应，也便于在登录后尽快恢复用户的本地状态。&#10;&#10;从架构层面看，登录功能位于 UI 层与数据层之间，由 ViewModel 负责输入校验、状态维护与事件分发，Repository 负责网络调用及本地持久化，环境切换则由统一配置类管理，相关实现可参见 `app/src/main/java/com/example/funnyexpensetracking/config/ApiEnvironmentConfig.kt`。&#10;&#10;在 ViewModel 层中，`LoginViewModel` 负责输入校验、状态切换以及登录成功后的同步触发，核心代码如上所示。Repository 层中，`AuthRepository` 定义认证契约，`AuthRepositoryImpl` 负责向后端发起登录请求并保存会话信息，完成令牌写入、用户信息持久化和退出登录后的会话清理。登录流程由此形成“校验、认证、持久化”的完整链路。&#10;&#10;```kotlin&#10;/**&#10; * 登录界面状态密封接口，定义四种互斥的界面状态。&#10; * Idle: 初始空闲状态；Loading: 认证请求进行中；&#10; * Success: 登录成功，即将触发数据同步；Error: 认证失败并携带提示信息。&#10; */&#10;sealed interface LoginUiState {&#10;    data object Idle : LoginUiState            // 表单初始状态，等待用户输入&#10;    data object Loading : LoginUiState         // 认证请求已发出，界面展示加载指示器&#10;    data object Success : LoginUiState         // 认证成功，即将跳转主界面并触发同步&#10;    data class Error(val message: String) : LoginUiState  // 认证失败，携带本地化错误信息&#10;}&#10;&#10;@HiltViewModel&#10;class LoginViewModel @Inject constructor(&#10;    private val authRepository: AuthRepository,&#10;    private val syncManager: com.example.funnyexpensetracking.data.sync.SyncManager&#10;) : ViewModel() {&#10;&#10;    private val _uiState = MutableStateFlow&lt;LoginUiState&gt;(LoginUiState.Idle)&#10;    val uiState: StateFlow&lt;LoginUiState&gt; = _uiState.asStateFlow()&#10;&#10;    /**&#10;     * 执行登录认证流程。&#10;     * 输入校验 → Loading 状态 → 远端认证 → Success/Error 状态。&#10;     * 成功时触发全量同步以恢复用户数据，失败时传递异常消息至 UI 层。&#10;     * @param email    用户邮箱，内部自动 trim&#10;     * @param password 明文密码&#10;     */&#10;    fun login(email: String, password: String) {&#10;        // 空输入提前拦截，避免无效网络请求&#10;        if (email.isBlank() || password.isBlank()) {&#10;            _uiState.value = LoginUiState.Error(&quot;请输入邮箱和密码&quot;)&#10;            return&#10;        }&#10;        viewModelScope.launch {&#10;            _uiState.value = LoginUiState.Loading            // 进入加载态&#10;            authRepository.login(email.trim(), password)&#10;                .onSuccess {&#10;                    _uiState.value = LoginUiState.Success     // 认证通过&#10;                    viewModelScope.launch { syncManager.syncAll() }  // 立即同步离线数据&#10;                }&#10;                .onFailure { e -&gt; _uiState.value = LoginUiState.Error(e.message ?: &quot;登录失败&quot;) }&#10;        }&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;/**&#10; * 认证仓库契约接口，定义登录、注册与登出操作。&#10; * 实现类负责向后端发起 HTTP 请求并管理本地会话持久化。&#10; */&#10;interface AuthRepository {&#10;    /** 用户登录：发送凭证至后端，成功返回 Unit，失败携带异常信息 */&#10;    suspend fun login(email: String, password: String): Result&lt;Unit&gt;&#10;    /** 用户注册：创建新账户并返回认证结果 */&#10;    suspend fun register(email: String, password: String, nickname: String): Result&lt;Unit&gt;&#10;    /** 登出操作：通知服务端并清除本地会话状态 */&#10;    suspend fun logout()&#10;}&#10;```&#10;&#10;```kotlin&#10;@Singleton&#10;class AuthRepositoryImpl @Inject constructor(&#10;    private val authApiService: AuthApiService,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) : AuthRepository {&#10;&#10;    /**&#10;     * 执行远端登录请求并持久化会话信息。&#10;     * 核心数据流：构造请求 → 发出 HTTP 调用 → 校验响应（HTTP 状态码 + 业务码 + 令牌非空）&#10;     * → 依次写入 authToken / refreshToken / userId / userEmail → 返回 Result。&#10;     * @param email    用户邮箱（内部 trim 处理）&#10;     * @param password 明文密码&#10;     * @return Result&lt;Unit&gt; 成功时携带 Unit，失败时携带服务端或网络异常&#10;     */&#10;    override suspend fun login(email: String, password: String): Result&lt;Unit&gt; {&#10;        return try {&#10;            val response = authApiService.login(&#10;                LoginRequestDto(&#10;                    email = email.trim(),&#10;                    password = password,&#10;                    deviceId = userPreferencesManager.getOrCreateDeviceId()  // 设备标识用于多端管理&#10;                )&#10;            )&#10;            val body = response.body()&#10;            // 三重校验：HTTP 成功 + 业务码 200 + 令牌非空&#10;            if (response.isSuccessful &amp;&amp; body?.code == 200 &amp;&amp; !body.data?.token.isNullOrBlank()) {&#10;                val data = requireNotNull(body.data)&#10;                // ↓ 持久化认证令牌链，供后续请求鉴权使用&#10;                userPreferencesManager.saveAuthToken(data.token)&#10;                if (!data.refreshToken.isNullOrBlank()) userPreferencesManager.saveRefreshToken(data.refreshToken)&#10;                data.user?.id?.let { userPreferencesManager.saveBackendUserId(it.toString()) }&#10;                data.user?.email?.let { userPreferencesManager.saveBackendUserEmail(it) }&#10;                Result.success(Unit)&#10;            } else {&#10;                // 业务层错误：提取服务端消息或 HTTP 状态描述&#10;                Result.failure(Exception(body?.message ?: response.message() ?: &quot;登录失败&quot;))&#10;            }&#10;        } catch (e: Exception) {&#10;            // 网络异常或 JSON 序列化失败&#10;            Result.failure(Exception(e.message ?: &quot;登录失败&quot;, e))&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 执行登出操作。&#10;     * 先尝试以 refreshToken 通知服务端注销会话；无论服务端是否响应，&#10;     * 均在 finally 块中清除本地所有会话数据，确保客户端状态一致性。&#10;     */&#10;    override suspend fun logout() {&#10;        try {&#10;            val refreshToken = userPreferencesManager.getRefreshToken()&#10;            if (refreshToken.isNotBlank()) {&#10;                authApiService.logout(LogoutRequestDto(refreshToken = refreshToken))  // 通知服务端&#10;            }&#10;        } finally {&#10;            // 无论网络请求是否成功，强制清除本地会话，防止残留令牌&#10;            userPreferencesManager.clearBackendSession()&#10;        }&#10;    }&#10;}&#10;```&#10;&#10;上述实现表明，登录模块通过输入校验、异步认证与本地会话保存的分层处理，既保证了界面交互的及时性，也让状态传播的边界更加清楚。&#10;&#10;5.1.2 日常记账模块实现&#10;&#10;日常记账模块主要实现收支记录的创建、修改与删除，并向界面提供聚合后的流水数据用于统计与展示。该模块采用离线优先策略，所有写操作优先落库并标记为待同步，待网络可用时再由同步管理器统一处理。UI 层通过 `TransactionViewModel` 暴露以日期或账户为粒度的 Flow，从而保持界面数据的实时更新。&#10;&#10;从架构上看，交易数据遵循领域模型与本地实体映射的约定，DAO 负责流式查询，RepositoryImpl 承担本地优先读写与同步标记，UseCase 层则封装分类汇总、预算预警等业务规则。ViewModel 将交易、账户与分类等数据源组合为 UIState，并通过事件流完成一次性消息分发。&#10;&#10;在 ViewModel 层中，`TransactionViewModel` 负责交易加载、同步状态监听和用户操作入口，核心代码如上所示。Repository 层中，`TransactionRepositoryImpl` 负责本地优先的增删改查与待同步标记，核心流程是先完成本地写入，再根据网络状态决定是否触发后台同步。&#10;&#10;```kotlin&#10;// 说明：该 ViewModel 负责加载当天交易、监听同步状态以及处理新增、删除等入口，核心流程围绕本地数据加载与同步触发展开。&#10;@HiltViewModel&#10;class TransactionViewModel @Inject constructor(&#10;    private val transactionUseCases: TransactionUseCases,&#10;    private val syncManager: SyncManager,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val realtimeAssetCalculator: RealtimeAssetCalculator,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) : BaseViewModel&lt;TransactionUiState, TransactionUiEvent&gt;() {&#10;    override fun initialState() = TransactionUiState()&#10;&#10;    init {&#10;        loadData()              // 加载当日交易与账户数据&#10;        initDefaultAccounts()   // 若账户表为空则初始化默认账户&#10;        observeSyncState()      // 监听同步状态与待同步数量&#10;        observeRealtimeAsset()  // 订阅实时资产流，驱动界面分钟级刷新&#10;    }&#10;&#10;    /**&#10;     * 添加一笔交易记录。&#10;     * 数据流：调用 UseCase 写入（内部标记待同步）→ 关闭对话框 → 发送成功事件&#10;     * → 根据网络状态决定是否立即触发全量同步。&#10;     * @param amount    交易金额&#10;     * @param type      收支类型（收入/支出）&#10;     * @param category  分类标签&#10;     * @param accountId 所属账户 ID&#10;     * @param note      备注（可选）&#10;     * @param date      交易时间戳，默认为当前时刻&#10;     */&#10;    fun addTransaction(&#10;        amount: Double,&#10;        type: TransactionType,&#10;        category: String,&#10;        accountId: Long,&#10;        note: String = &quot;&quot;,&#10;        date: Long = System.currentTimeMillis()&#10;    ) {&#10;        viewModelScope.launch {&#10;            transactionUseCases.addTransaction(&#10;                amount = amount,&#10;                type = type,&#10;                category = category,&#10;                accountId = accountId,&#10;                note = note,&#10;                date = date&#10;            )&#10;            hideAddDialog()&#10;            sendEvent(TransactionUiEvent.TransactionAdded)&#10;            // 根据网络状态提供差异化提示&#10;            sendEvent(TransactionUiEvent.ShowMessage(if (networkMonitor.isNetworkAvailable()) &quot;记账成功&quot; else &quot;记账成功（离线模式，稍后自动同步）&quot;))&#10;            if (networkMonitor.isNetworkAvailable()) syncManager.syncAll()  // 在线时立即同步&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 删除指定交易记录，成功后触发同步。&#10;     * @param transaction 待删除的交易领域对象&#10;     */&#10;    fun deleteTransaction(transaction: Transaction) {&#10;        viewModelScope.launch {&#10;            transactionUseCases.deleteTransaction(transaction)&#10;            sendEvent(TransactionUiEvent.TransactionDeleted)&#10;            sendEvent(TransactionUiEvent.ShowMessage(&quot;删除成功&quot;))&#10;            if (networkMonitor.isNetworkAvailable()) syncManager.syncAll()&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 订阅 RealtimeAssetCalculator 输出的实时资产流，&#10;     * 将 currentAsset / incomePerMinute / expensePerMinute / netChangePerMinute 映射到 UIState。&#10;     */&#10;    private fun observeRealtimeAsset() {&#10;        realtimeAssetCalculator.realtimeAsset.onEach { assetData -&gt;&#10;            updateState {&#10;                copy(&#10;                    realtimeAsset = assetData.currentAsset,&#10;                    incomePerMinute = assetData.incomePerMinute,&#10;                    expensePerMinute = assetData.expensePerMinute,&#10;                    netChangePerMinute = assetData.netChangePerMinute&#10;                )&#10;            }&#10;        }.launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 监听 SyncManager 的同步状态与待同步计数。&#10;     * 同步成功时向用户展示已同步条数，失败时展示错误信息。&#10;     */&#10;    private fun observeSyncState() {&#10;        syncManager.syncState.onEach { state -&gt;&#10;            updateState { copy(isSyncing = state is SyncState.Syncing, isOffline = !networkMonitor.isNetworkAvailable()) }&#10;            when (state) {&#10;                is SyncState.Success -&gt; if (state.syncedCount &gt; 0) sendEvent(TransactionUiEvent.ShowMessage(&quot;同步完成，共同步 ${state.syncedCount} 条记录&quot;))&#10;                is SyncState.Error -&gt; sendEvent(TransactionUiEvent.ShowMessage(&quot;同步失败: ${state.message}&quot;))&#10;                else -&gt; {}&#10;            }&#10;        }.launchIn(viewModelScope)&#10;        syncManager.pendingSyncCount.onEach { count -&gt; updateState { copy(pendingSyncCount = count) } }.launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 若本地账户表为空，则创建预设默认账户（如现金、银行卡等），&#10;     * 保证首次使用时的记账体验。&#10;     */&#10;    private fun initDefaultAccounts() {&#10;        viewModelScope.launch { transactionUseCases.initializeDefaultAccountsIfEmpty() }&#10;    }&#10;&#10;    /**&#10;     * 加载当日交易数据，取今日起止时间戳，通过 UseCase 层获取聚合结果后填充 UIState。&#10;     * 数据源通过 Flow 持续推送，界面随本地数据变更自动刷新。&#10;     */&#10;    private fun loadData() {&#10;        updateState { copy(loadingState = LoadingState.LOADING) }&#10;        val todayStart = DateTimeUtil.getTodayStartTimestamp()&#10;        val todayEnd = DateTimeUtil.getTodayEndTimestamp()&#10;        transactionUseCases.loadTransactionsByDateRange(todayStart, todayEnd).onEach { result -&gt;&#10;            updateState {&#10;                copy(&#10;                    transactions = result.transactions,&#10;                    dailyTransactions = result.dailyTransactions,&#10;                    accounts = result.accounts,&#10;                    todayIncome = result.todayIncome,&#10;                    todayExpense = result.todayExpense,&#10;                    totalBalance = result.totalBalance,&#10;                    loadingState = LoadingState.SUCCESS&#10;                )&#10;            }&#10;        }.launchIn(viewModelScope)&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该仓库实现负责将交易记录写入本地数据库并在网络可用时触发同步，逻辑重点是本地优先与同步状态维护。&#10;@Singleton&#10;class TransactionRepositoryImpl @Inject constructor(&#10;    private val transactionDao: TransactionDao,&#10;    private val expenseApiService: ExpenseApiService,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val syncManager: SyncManager&#10;) : TransactionRepository {&#10;    // 非核心查询方法省略，核心写入与同步流程完整展示&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 新增交易记录：领域对象转换为实体并标记 PENDING_UPLOAD，先写入本地保证离线可用，&#10;     * 随后在网络可用时触发后台同步。&#10;     * @param transaction 交易领域对象&#10;     * @return 本地生成的记录 ID&#10;     */&#10;    override suspend fun addTransaction(transaction: Transaction): Long {&#10;        // 先写本地并标记待上传，保证离线可用&#10;        val entity = toEntity(transaction).copy(&#10;            syncStatus = SyncStatus.PENDING_UPLOAD,&#10;            updatedAt = System.currentTimeMillis()&#10;        )&#10;        val localId = transactionDao.insert(entity)&#10;&#10;        // 网络可用时立即尝试同步，减少待同步积压&#10;        trySync()&#10;        return localId&#10;    }&#10;&#10;    /**&#10;     * 更新交易记录：保留原有 serverId，将同步状态重置为 PENDING_UPLOAD，&#10;     * 确保后续同步流程能将本地修改上传至后端。&#10;     * @param transaction 包含新值的交易领域对象&#10;     */&#10;    override suspend fun updateTransaction(transaction: Transaction) {&#10;        val existingEntity = transactionDao.getById(transaction.id)&#10;        // 更新后统一回到待上传状态，由同步管理器处理与后端的一致性&#10;        val entity = toEntity(transaction).copy(&#10;            serverId = existingEntity?.serverId,&#10;            syncStatus = SyncStatus.PENDING_UPLOAD,&#10;            updatedAt = System.currentTimeMillis()&#10;        )&#10;        transactionDao.update(entity)&#10;        trySync()&#10;    }&#10;&#10;    /**&#10;     * 删除交易记录：区分&quot;已同步到后端&quot;与&quot;仅存在于本地&quot;两种情况。&#10;     * 已同步的记录执行软删除标记，待后端确认后再物理删除；&#10;     * 未同步的记录直接物理删除，避免产生孤儿同步请求。&#10;     * @param transaction 待删除的交易领域对象&#10;     */&#10;    override suspend fun deleteTransaction(transaction: Transaction) {&#10;        val entity = transactionDao.getById(transaction.id)&#10;        if (entity != null) {&#10;            if (entity.serverId != null) {&#10;                // 已上云数据仅做软删除标记，待后端删除成功后再清理本地&#10;                transactionDao.markAsDeleted(transaction.id)&#10;                trySync()&#10;            } else {&#10;                // 本地未上云数据可直接删除&#10;                transactionDao.delete(entity)&#10;            }&#10;        }&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 网络可用时触发交易同步，作为各写入方法的后置钩子，&#10;     * 减少待同步数据积压并降低跨端冲突概率。&#10;     */&#10;    private suspend fun trySync() {&#10;        if (networkMonitor.isNetworkAvailable()) {&#10;            syncManager.syncTransactions()&#10;        }&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;}&#10;```&#10;&#10;上述实现体现了本地优先的数据写入策略，也说明了通过观察流式数据来更新界面的常见做法。&#10;&#10;5.1.3 固定收支模块实现&#10;&#10;固定收支模块主要实现周期性收入与支出的管理，并按既定频率生成收支计划或参与实时资产计算。实现上，固定收支实体包含频率、起止时间和累计规则等字段，DAO 提供时间范围查询接口，Domain 层则提供下一次发生时间及周期内累计金额的计算方法。系统中的周期性计算器会定期读取固定收支条目，并将结果汇总到实时资产快照中。&#10;&#10;从实现逻辑上看，模块的关键在于周期计算边界处理、多频率统一换算，以及固定收支变化对资产计算的联动。下面的两段代码分别展示 `RealtimeAssetCalculator` 的分钟级累计更新逻辑，以及 `AssetRepositoryImpl` 对固定收支数据的持久化与更新承接。&#10;&#10;```kotlin&#10;// 说明：该计算器负责监听账户、固定收支和投资价值变化，并按分钟更新累计金额后重算实时资产。&#10;@Singleton&#10;class RealtimeAssetCalculator @Inject constructor(&#10;    private val assetRepository: AssetRepository,&#10;    private val accountRepository: AccountRepository,&#10;    private val assetBaselineDao: AssetBaselineDao,&#10;    private val investmentRepository: InvestmentRepository&#10;) {&#10;    private val scope = CoroutineScope(Dispatchers.Default + Job())&#10;&#10;    /**&#10;     * MutableStateFlow 持有当前实时资产快照，供 ViewModel 层订阅。&#10;     * 包含 currentAsset（总资产）、perMinute 指标（分钟级收支速率）、baseline 信息等。&#10;     */&#10;    private val _realtimeAsset = MutableStateFlow(RealtimeAssetData(&#10;        currentAsset = 0.0,&#10;        incomePerMinute = 0.0,&#10;        expensePerMinute = 0.0,&#10;        netChangePerMinute = 0.0,&#10;        baselineTimestamp = 0L,&#10;        baselineAmount = 0.0&#10;    ))&#10;    val realtimeAsset: StateFlow&lt;RealtimeAssetData&gt; = _realtimeAsset.asStateFlow()&#10;&#10;    init {&#10;        // observe account, fixed income &amp; investment changes → trigger recalculateAsset()&#10;        // start per-minute fixed income accumulation update&#10;        scope.launch { recalculateAsset() }&#10;    }&#10;&#10;    /* observation &amp; periodic accumulation control logic omitted for brevity */&#10;&#10;    /**&#10;     * 为指定固定收支条目计算自上次记录以来的分钟级累计。&#10;     * 核心流程：确定有效起始时间 → 计算增量分钟数 → 更新累计分钟与金额 → 持久化。&#10;     * 若首次记录（lastRecordTime == 0），以 startDate 和当前时间减一分钟中的较大者作为起始点；&#10;     * 若增量分钟数 ≤ 0，仅更新 lastRecordTime 而不累加金额，防止重复计数。&#10;     * @param fixedIncome 固定收支条目&#10;     * @param currentTime 当前分钟级时间戳&#10;     */&#10;    private suspend fun updateFixedIncomeAccumulation(&#10;        fixedIncome: FixedIncome,&#10;        currentTime: Long&#10;    ) {&#10;        val lastTime = fixedIncome.lastRecordTime&#10;        // 确定累计起始时间：首次记录时取 max(startDate, currentTime - 1min)，否则取上次记录时间&#10;        val effectiveLastTime = if (lastTime == 0L) {&#10;            maxOf(fixedIncome.startDate, currentTime - 60_000)&#10;        } else {&#10;            lastTime&#10;        }&#10;&#10;        val deltaMinutes = (currentTime - effectiveLastTime) / 60_000  // 距上次记录的分钟增量&#10;&#10;        if (deltaMinutes &lt;= 0) {&#10;            // 无增量，仅推进 lastRecordTime 以避免后续重复计算&#10;            assetRepository.updateFixedIncomeAccumulation(&#10;                id = fixedIncome.id,&#10;                accumulatedMinutes = fixedIncome.accumulatedMinutes,&#10;                accumulatedAmount = fixedIncome.accumulatedAmount,&#10;                lastRecordTime = currentTime&#10;            )&#10;            return&#10;        }&#10;&#10;        // 计算有效分钟数（剔除条目未激活或在起止时间之外的部分）&#10;        val effectiveMinutes = calculateEffectiveMinutes(&#10;            fixedIncome = fixedIncome,&#10;            fromTime = effectiveLastTime,&#10;            toTime = currentTime&#10;        )&#10;&#10;        val newAccumulatedMinutes = fixedIncome.accumulatedMinutes + effectiveMinutes&#10;&#10;        // 按条目频率将累计分钟数换算为累计金额&#10;        val newAccumulatedAmount = fixedIncome.frequency.calculateAccumulatedAmount(&#10;            fixedIncome.amount,&#10;            newAccumulatedMinutes&#10;        )&#10;&#10;        assetRepository.updateFixedIncomeAccumulation(&#10;            id = fixedIncome.id,&#10;            accumulatedMinutes = newAccumulatedMinutes,&#10;            accumulatedAmount = newAccumulatedAmount,&#10;            lastRecordTime = currentTime&#10;        )&#10;    }&#10;&#10;    /* calculateEffectiveMinutes and other helpers omitted for brevity */&#10;&#10;    /**&#10;     * 重算实时资产并发布到 StateFlow。&#10;     * 数据汇聚路径：账户余额 + 固定收支净累计 + 投资当前市值 → 总资产；&#10;     * 遍历生效中的固定收支条目计算分钟级收入/支出速率，写入 AssetBaseline 并刷新 _realtimeAsset。&#10;     */&#10;    fun recalculateAsset() {&#10;        scope.launch {&#10;            val currentMinuteTimestamp = getCurrentMinuteTimestamp()&#10;&#10;            // 1. 聚合各数据源&#10;            val totalAccountBalance = accountRepository.getTotalBalance()&#10;&#10;            val totalFixedIncome = assetRepository.getTotalAccumulatedIncome()&#10;            val totalFixedExpense = assetRepository.getTotalAccumulatedExpense()&#10;            val fixedIncomeNetAmount = totalFixedIncome - totalFixedExpense  // 固定收支净额&#10;&#10;            val totalInvestmentValue = investmentRepository.getTotalCurrentValue()&#10;&#10;            // 2. 计算总资产 = 账户余额 + 固定收支净额 + 投资市值&#10;            val currentAsset = totalAccountBalance + fixedIncomeNetAmount + totalInvestmentValue&#10;&#10;            // 3. 遍历激活条目计算分钟速率&#10;            val fixedIncomes = assetRepository.getAllFixedIncomesList()&#10;            var incomePerMinute = 0.0&#10;            var expensePerMinute = 0.0&#10;&#10;            fixedIncomes.forEach { entity -&gt;&#10;                if (entity.isEffectiveAt(currentMinuteTimestamp)) {&#10;                    val perMinute = entity.getAmountPerMinute()&#10;                    if (entity.type == FixedIncomeType.INCOME) {&#10;                        incomePerMinute += perMinute&#10;                    } else {&#10;                        expensePerMinute += perMinute&#10;                    }&#10;                }&#10;            }&#10;&#10;            val netChangePerMinute = incomePerMinute - expensePerMinute&#10;&#10;            // 4. 写入基线快照，供后续偏移计算&#10;            val newBaseline = AssetBaselineEntity(&#10;                id = 1,&#10;                baselineTimestamp = currentMinuteTimestamp,&#10;                baselineAmount = currentAsset&#10;            )&#10;            assetBaselineDao.insert(newBaseline)&#10;&#10;            // 5. 发布最新资产数据&#10;            _realtimeAsset.value = RealtimeAssetData(&#10;                currentAsset = currentAsset,&#10;                incomePerMinute = incomePerMinute,&#10;                expensePerMinute = expensePerMinute,&#10;                netChangePerMinute = netChangePerMinute,&#10;                baselineTimestamp = currentMinuteTimestamp,&#10;                baselineAmount = currentAsset,&#10;                totalAccountBalance = totalAccountBalance,&#10;                totalFixedIncome = totalFixedIncome,&#10;                totalFixedExpense = totalFixedExpense,&#10;                totalInvestmentValue = totalInvestmentValue&#10;            )&#10;        }&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该仓库负责固定收支的增删改与累计值落库，整体逻辑是将领域对象映射为实体后交由 DAO 持久化。&#10;@Singleton&#10;class AssetRepositoryImpl @Inject constructor(&#10;    private val fixedIncomeDao: FixedIncomeDao,&#10;    private val assetSnapshotDao: AssetSnapshotDao,&#10;    private val transactionDao: TransactionDao,&#10;    private val stockHoldingDao: StockHoldingDao,&#10;    private val investmentDao: InvestmentDao&#10;) : AssetRepository {&#10;&#10;    // ────────── 固定收支 CRUD ──────────&#10;&#10;    /** 获取所有活跃的固定收支条目，以 Flow 形式返回领域对象列表 */&#10;    override fun getAllActiveFixedIncomes(): Flow&lt;List&lt;FixedIncome&gt;&gt; {&#10;        return fixedIncomeDao.getAllActiveFixedIncomes().map { entities -&gt;&#10;            entities.map { it.toDomainModel() }&#10;        }&#10;    }&#10;&#10;    /** 获取全部固定收支条目（含非活跃），以 Flow 形式返回 */&#10;    override fun getAllFixedIncomes(): Flow&lt;List&lt;FixedIncome&gt;&gt; {&#10;        return fixedIncomeDao.getAllFixedIncomes().map { entities -&gt;&#10;            entities.map { it.toDomainModel() }&#10;        }&#10;    }&#10;&#10;    /** 按收支类型筛选活跃条目 */&#10;    override fun getFixedIncomesByType(type: FixedIncomeType): Flow&lt;List&lt;FixedIncome&gt;&gt; {&#10;        return fixedIncomeDao.getActiveByType(type.toEntityType()).map { entities -&gt;&#10;            entities.map { it.toDomainModel() }&#10;        }&#10;    }&#10;&#10;    /** 新增固定收支条目，返回本地生成 ID */&#10;    override suspend fun addFixedIncome(fixedIncome: FixedIncome): Long {&#10;        return fixedIncomeDao.insert(fixedIncome.toEntity())&#10;    }&#10;&#10;    /** 更新固定收支条目 */&#10;    override suspend fun updateFixedIncome(fixedIncome: FixedIncome) {&#10;        fixedIncomeDao.update(fixedIncome.toEntity())&#10;    }&#10;&#10;    /** 删除固定收支条目 */&#10;    override suspend fun deleteFixedIncome(fixedIncome: FixedIncome) {&#10;        fixedIncomeDao.delete(fixedIncome.toEntity())&#10;    }&#10;&#10;    /** 更新固定收支条目的启用/停用状态 */&#10;    override suspend fun updateFixedIncomeStatus(id: Long, isActive: Boolean) {&#10;        fixedIncomeDao.updateActiveStatus(id, isActive)&#10;    }&#10;&#10;    // ────────── 累计值查询 ──────────&#10;&#10;    /** 查询所有固定收入累计总金额 */&#10;    override suspend fun getTotalAccumulatedIncome(): Double {&#10;        return fixedIncomeDao.getTotalAccumulatedIncome()&#10;    }&#10;&#10;    /** 查询所有固定支出累计总金额 */&#10;    override suspend fun getTotalAccumulatedExpense(): Double {&#10;        return fixedIncomeDao.getTotalAccumulatedExpense()&#10;    }&#10;&#10;    /** 获取全部固定收支列表（挂起函数版本） */&#10;    override suspend fun getAllFixedIncomesList(): List&lt;FixedIncome&gt; {&#10;        return fixedIncomeDao.getAllFixedIncomesList().map { it.toDomainModel() }&#10;    }&#10;&#10;    /** 更新指定条目的累计分钟数、累计金额与最后记录时间 */&#10;    override suspend fun updateFixedIncomeAccumulation(&#10;        id: Long,&#10;        accumulatedMinutes: Long,&#10;        accumulatedAmount: Double,&#10;        lastRecordTime: Long&#10;    ) {&#10;        fixedIncomeDao.updateAccumulated(id, accumulatedMinutes, accumulatedAmount, lastRecordTime)&#10;    }&#10;&#10;    // ────────── 资产快照 ──────────&#10;&#10;    /** 保存资产快照 */&#10;    override suspend fun saveAssetSnapshot(snapshot: AssetSnapshot): Long {&#10;        return assetSnapshotDao.insert(snapshot.toEntity())&#10;    }&#10;&#10;    /** 获取最近一次快照 */&#10;    override suspend fun getLatestSnapshot(): AssetSnapshot? {&#10;        return assetSnapshotDao.getLatestSnapshot()?.toDomainModel()&#10;    }&#10;&#10;    /** 以 Flow 形式订阅最新快照 */&#10;    override fun getLatestSnapshotFlow(): Flow&lt;AssetSnapshot?&gt; {&#10;        return assetSnapshotDao.getLatestSnapshotFlow().map { it?.toDomainModel() }&#10;    }&#10;&#10;    /** 按时间范围查询快照列表 */&#10;    override fun getSnapshotsByTimeRange(startTime: Long, endTime: Long): Flow&lt;List&lt;AssetSnapshot&gt;&gt; {&#10;        return assetSnapshotDao.getSnapshotsByTimeRange(startTime, endTime).map { entities -&gt;&#10;            entities.map { it.toDomainModel() }&#10;        }&#10;    }&#10;&#10;    /** 获取最近 N 条快照 */&#10;    override fun getRecentSnapshots(limit: Int): Flow&lt;List&lt;AssetSnapshot&gt;&gt; {&#10;        return assetSnapshotDao.getRecentSnapshots(limit).map { entities -&gt;&#10;            entities.map { it.toDomainModel() }&#10;        }&#10;    }&#10;&#10;    // ────────── 核心：资产汇总计算 ──────────&#10;&#10;    /**&#10;     * 计算当前资产汇总摘要。&#10;     * 数据汇聚维度：&#10;     *   1. 交易收支（transactionDao）：现金流入/流出合计 → cashAsset&#10;     *   2. 固定收支（fixedIncomeDao）：分钟级固定收入/支出速率 → fixedIncomePerMinute / fixedExpensePerMinute&#10;     *   3. 投资组合（stockHoldingDao + investmentDao）：股票与投资当前市值与成本 → stockAsset / stockProfitLoss&#10;     * 总资产 = cashAsset + stockAsset。&#10;     * @return AssetSummary 包含分层资产与分钟速率指标&#10;     */&#10;    override suspend fun calculateCurrentAssetSummary(): AssetSummary {&#10;        val currentTime = System.currentTimeMillis()&#10;&#10;        // 1. 统计交易流水&#10;        val totalIncome = transactionDao.getTotalByTypeAndDateRange(&#10;            EntityTransactionType.INCOME, 0, currentTime&#10;        ) ?: 0.0&#10;        val totalExpense = transactionDao.getTotalByTypeAndDateRange(&#10;            EntityTransactionType.EXPENSE, 0, currentTime&#10;        ) ?: 0.0&#10;&#10;        // 2. 统计活跃固定收支的分钟速率&#10;        val fixedIncomes = fixedIncomeDao.getAllActiveFixedIncomes().first()&#10;        var fixedIncomePerMinute = 0.0&#10;        var fixedExpensePerMinute = 0.0&#10;&#10;        fixedIncomes.forEach { entity -&gt;&#10;            val amountPerMinute = calculateAmountPerMinute(entity.amount, entity.frequency)&#10;            if (entity.type == EntityFixedIncomeType.INCOME) {&#10;                fixedIncomePerMinute += amountPerMinute&#10;            } else {&#10;                fixedExpensePerMinute += amountPerMinute&#10;            }&#10;        }&#10;&#10;        // 3. 统计投资组合&#10;        val stockValue = stockHoldingDao.getTotalStockValue() ?: 0.0&#10;        val stockCost = stockHoldingDao.getTotalStockCost() ?: 0.0&#10;        val investmentCurrentValue = investmentDao.getTotalCurrentValue()&#10;        val investmentCost = investmentDao.getTotalInvestment()&#10;&#10;        val totalStockAsset = stockValue + investmentCurrentValue&#10;        val totalStockCost = stockCost + investmentCost&#10;        val stockProfitLoss = totalStockAsset - totalStockCost  // 浮动盈亏&#10;        val cashAsset = totalIncome - totalExpense&#10;&#10;        return AssetSummary(&#10;            totalAsset = cashAsset + totalStockAsset,          // 总资产 = 现金 + 投资&#10;            cashAsset = cashAsset,&#10;            stockAsset = totalStockAsset,&#10;            totalIncome = totalIncome,&#10;            totalExpense = totalExpense,&#10;            fixedIncomePerMinute = fixedIncomePerMinute,&#10;            fixedExpensePerMinute = fixedExpensePerMinute,&#10;            netIncomePerMinute = fixedIncomePerMinute - fixedExpensePerMinute,&#10;            stockProfitLoss = stockProfitLoss,&#10;            timestamp = currentTime&#10;        )&#10;    }&#10;&#10;    /**&#10;     * 以 60 秒为周期持续产出资产摘要流，&#10;     * 供实时资产监控界面订阅。&#10;     */&#10;    override fun getAssetSummaryFlow(): Flow&lt;AssetSummary&gt; {&#10;        return flow {&#10;            while (true) {&#10;                emit(calculateCurrentAssetSummary())&#10;                delay(60_000)&#10;            }&#10;        }&#10;    }&#10;&#10;    // ────────── 辅助方法 ──────────&#10;&#10;    /**&#10;     * 按固定收支频率将金额换算为分钟级速率。&#10;     * 换算基准：每天 1440 分钟、每周 10080 分钟、每月 43200 分钟、每年 525600 分钟。&#10;     * @param amount    周期金额&#10;     * @param frequency 收支频率枚举&#10;     * @return 分钟级金额速率&#10;     */&#10;    private fun calculateAmountPerMinute(amount: Double, frequency: EntityFixedIncomeFrequency): Double {&#10;        return when (frequency) {&#10;            EntityFixedIncomeFrequency.DAILY -&gt; amount / (24 * 60)&#10;            EntityFixedIncomeFrequency.WEEKLY -&gt; amount / (7 * 24 * 60)&#10;            EntityFixedIncomeFrequency.MONTHLY -&gt; amount / (30 * 24 * 60)&#10;            EntityFixedIncomeFrequency.YEARLY -&gt; amount / (365 * 24 * 60)&#10;        }&#10;    }&#10;&#10;    // ────────── 实体 ↔ 领域模型映射 ──────────&#10;&#10;    /** FixedIncomeEntity → FixedIncome 领域模型映射 */&#10;    private fun toDomainModel(entity: FixedIncomeEntity): FixedIncome {&#10;        return FixedIncome(&#10;            id = entity.id,&#10;            name = entity.name,&#10;            amount = entity.amount,&#10;            type = entity.type.toDomainType(),&#10;            frequency = entity.frequency.toDomainFrequency(),&#10;            startDate = entity.startDate,&#10;            endDate = entity.endDate,&#10;            isActive = entity.isActive,&#10;            accumulatedMinutes = entity.accumulatedMinutes,&#10;            accumulatedAmount = entity.accumulatedAmount,&#10;            lastRecordTime = entity.lastRecordTime&#10;        )&#10;    }&#10;&#10;    /** FixedIncome 领域模型 → FixedIncomeEntity 实体映射 */&#10;    private fun toEntity(fixedIncome: FixedIncome): FixedIncomeEntity {&#10;        return FixedIncomeEntity(&#10;            id = fixedIncome.id,&#10;            name = fixedIncome.name,&#10;            amount = fixedIncome.amount,&#10;            type = fixedIncome.type.toEntityType(),&#10;            frequency = fixedIncome.frequency.toEntityFrequency(),&#10;            startDate = fixedIncome.startDate,&#10;            endDate = fixedIncome.endDate,&#10;            isActive = fixedIncome.isActive,&#10;            accumulatedMinutes = fixedIncome.accumulatedMinutes,&#10;            accumulatedAmount = fixedIncome.accumulatedAmount,&#10;            lastRecordTime = fixedIncome.lastRecordTime&#10;        )&#10;    }&#10;&#10;    /** AssetSnapshotEntity → AssetSnapshot 映射 */&#10;    private fun toDomainModel(entity: AssetSnapshotEntity): AssetSnapshot {&#10;        return AssetSnapshot(&#10;            id = entity.id,&#10;            totalAsset = entity.totalAsset,&#10;            cashAsset = entity.cashAsset,&#10;            stockAsset = entity.stockAsset,&#10;            timestamp = entity.timestamp&#10;        )&#10;    }&#10;&#10;    /** AssetSnapshot → AssetSnapshotEntity 映射 */&#10;    private fun toEntity(snapshot: AssetSnapshot): AssetSnapshotEntity {&#10;        return AssetSnapshotEntity(&#10;            id = snapshot.id,&#10;            totalAsset = snapshot.totalAsset,&#10;            cashAsset = snapshot.cashAsset,&#10;            stockAsset = snapshot.stockAsset,&#10;            timestamp = snapshot.timestamp&#10;        )&#10;    }&#10;&#10;    /** EntityFixedIncomeType → FixedIncomeType 枚举映射 */&#10;    private fun EntityFixedIncomeType.toDomainType(): FixedIncomeType { return when (this) { EntityFixedIncomeType.INCOME -&gt; FixedIncomeType.INCOME; EntityFixedIncomeType.EXPENSE -&gt; FixedIncomeType.EXPENSE } }&#10;&#10;    /** FixedIncomeType → EntityFixedIncomeType 枚举映射 */&#10;    private fun FixedIncomeType.toEntityType(): EntityFixedIncomeType { return when (this) { FixedIncomeType.INCOME -&gt; EntityFixedIncomeType.INCOME; FixedIncomeType.EXPENSE -&gt; EntityFixedIncomeType.EXPENSE } }&#10;&#10;    /** EntityFixedIncomeFrequency → FixedIncomeFrequency 枚举映射 */&#10;    private fun EntityFixedIncomeFrequency.toDomainFrequency(): FixedIncomeFrequency { return when (this) { EntityFixedIncomeFrequency.DAILY -&gt; FixedIncomeFrequency.DAILY; EntityFixedIncomeFrequency.WEEKLY -&gt; FixedIncomeFrequency.WEEKLY; EntityFixedIncomeFrequency.MONTHLY -&gt; FixedIncomeFrequency.MONTHLY; EntityFixedIncomeFrequency.YEARLY -&gt; FixedIncomeFrequency.YEARLY } }&#10;&#10;    /** FixedIncomeFrequency → EntityFixedIncomeFrequency 枚举映射 */&#10;    private fun FixedIncomeFrequency.toEntityFrequency(): EntityFixedIncomeFrequency { return when (this) { FixedIncomeFrequency.DAILY -&gt; EntityFixedIncomeFrequency.DAILY; FixedIncomeFrequency.WEEKLY -&gt; EntityFixedIncomeFrequency.WEEKLY; FixedIncomeFrequency.MONTHLY -&gt; EntityFixedIncomeFrequency.MONTHLY; FixedIncomeFrequency.YEARLY -&gt; EntityFixedIncomeFrequency.YEARLY } }&#10;}&#10;```&#10;&#10;上述实现说明，固定收支模块通过周期性累计更新与实时资产重算，维持了时间维度上资产统计的一致性。&#10;&#10;5.1.4 投资管理模块实现&#10;&#10;投资管理模块主要实现投资条目的记录、组合汇总、外部行情拉取与周期性市值刷新。架构上，投资数据通过 `InvestmentRepository` 暴露给 ViewModel，Repository 负责缓存本地数据，并在需要时向后端或第三方行情接口发起请求。为降低界面等待时间，该模块采用异步刷新策略，先展示本地缓存的市值，再在后台完成行情更新与结果回写。&#10;&#10;从实现层面看，需要关注行情请求的限流与去重、不同网络条件下的降级处理，以及价格更新与资产快照之间的一致性。下面的两段代码分别展示 `InvestmentViewModel` 的条目合并与刷新触发逻辑，以及 `InvestmentRepositoryImpl` 的行情拉取与本地更新过程。&#10;&#10;```kotlin&#10;// 说明：该 ViewModel 负责加载投资数据、合并同类条目，并周期性触发股票价格刷新，整体逻辑围绕本地展示和异步行情更新展开。&#10;@HiltViewModel&#10;class InvestmentViewModel @Inject constructor(&#10;    private val investmentDao: InvestmentDao,&#10;    private val investmentRepository: InvestmentRepository&#10;) : BaseViewModel&lt;InvestmentUiState, InvestmentUiEvent&gt;() {&#10;&#10;    private var stockPriceRefreshJob: Job? = null&#10;&#10;    override fun initialState() = InvestmentUiState()&#10;&#10;    init {&#10;        loadInvestments()           // 加载本地投资数据&#10;        startStockPriceRefresh()    // 启动周期性行情刷新&#10;    }&#10;&#10;    /**&#10;     * 从 DAO 加载全部投资条目并进行聚合。&#10;     * 流程：DAO Flow → 实体映射为领域对象 → 同品类合并 → 计算总投入/总市值/总盈亏 → 写入 UIState。&#10;     */&#10;    private fun loadInvestments() {&#10;        updateState { copy(loadingState = LoadingState.LOADING) }&#10;&#10;        investmentDao.getAllInvestments()&#10;            .onEach { entities -&gt;&#10;                val investments = entities.map { toDomainModel(it) }&#10;&#10;                val mergedInvestments = mergeInvestments(investments)&#10;&#10;                val totalInvestment = mergedInvestments.sumOf { it.investment }&#10;                val totalCurrentValue = mergedInvestments.sumOf { it.calcCurrentValue() }&#10;                val totalProfitLoss = totalCurrentValue - totalInvestment&#10;&#10;                updateState {&#10;                    copy(&#10;                        allInvestments = mergedInvestments,&#10;                        filteredInvestments = applyFilter(mergedInvestments, filterType),&#10;                        totalInvestment = totalInvestment,&#10;                        totalCurrentValue = totalCurrentValue,&#10;                        totalProfitLoss = totalProfitLoss,&#10;                        loadingState = LoadingState.SUCCESS&#10;                    )&#10;                }&#10;            }&#10;            .launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 按品类与描述将同名投资条目合并，累加数量、投入成本与当前市值。&#10;     * 分组键为 &quot;category_description&quot;，单条目直接返回，多条目求和后返回副本。&#10;     * @param investments 原始投资列表&#10;     * @return 合并后的投资列表&#10;     */&#10;    private fun mergeInvestments(investments: List&lt;Investment&gt;): List&lt;Investment&gt; {&#10;        return investments&#10;            .groupBy { &quot;${it.category}_${it.description}&quot; }  // 按品类+描述分组&#10;            .map { (_, items) -&gt;&#10;                if (items.size == 1) {&#10;                    items.first()&#10;                } else {&#10;                    val first = items.first()&#10;                    first.copy(&#10;                        quantity = items.sumOf { it.quantity },&#10;                        investment = items.sumOf { it.investment },&#10;                        currentValue = items.sumOf { it.currentValue }&#10;                    )&#10;                }&#10;            }&#10;    }&#10;&#10;    /**&#10;     * 设置筛选类型并立即应用过滤，用于界面切换&quot;全部/股票/其他&quot;视图。&#10;     * @param filterType 筛选枚举值&#10;     */&#10;    fun setFilterType(filterType: InvestmentFilterType) {&#10;        updateState {&#10;            copy(&#10;                filterType = filterType,&#10;                filteredInvestments = applyFilter(allInvestments, filterType)&#10;            )&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 根据筛选类型过滤投资列表。&#10;     * ALL 返回全量，STOCK 仅保留股票类，OTHER 仅保留其他品类。&#10;     */&#10;    private fun applyFilter(list: List&lt;Investment&gt;, filterType: InvestmentFilterType): List&lt;Investment&gt; {&#10;        return when (filterType) {&#10;            InvestmentFilterType.ALL -&gt; list&#10;            InvestmentFilterType.STOCK -&gt; list.filter { it.category == InvestmentCategory.STOCK }&#10;            InvestmentFilterType.OTHER -&gt; list.filter { it.category == InvestmentCategory.OTHER }&#10;        }&#10;    }&#10;&#10;    /** 显示添加对话框，同时清空编辑目标 */&#10;    fun showAddDialog() {&#10;        updateState { copy(showAddDialog = true, editingInvestment = null) }&#10;    }&#10;&#10;    /** 隐藏对话框并发送关闭事件 */&#10;    fun hideAddDialog() {&#10;        updateState { copy(showAddDialog = false, editingInvestment = null) }&#10;        sendEvent(InvestmentUiEvent.DismissDialog)&#10;    }&#10;&#10;    /** 将指定投资条目设为编辑目标并打开对话框 */&#10;    fun editInvestment(investment: Investment) {&#10;        updateState { copy(showAddDialog = true, editingInvestment = investment) }&#10;    }&#10;&#10;    /**&#10;     * 新增投资条目。若已存在同描述同品类的条目，则合并数量与投入；否则插入新行。&#10;     * 股票类投资（STOCK）的 currentValue 由行情刷新驱动，此处仅设投入值；&#10;     * 其他类投资（OTHER）则同步设置 currentValue。&#10;     * @param category     品类（STOCK/OTHER）&#10;     * @param description  描述/名称&#10;     * @param quantity     持有数量&#10;     * @param investment   投入成本&#10;     * @param currentValue 当前市值（非股票类可选指定）&#10;     */&#10;    fun addInvestment(&#10;        category: InvestmentCategory,&#10;        description: String,&#10;        quantity: Double,&#10;        investment: Double,&#10;        currentValue: Double? = null&#10;    ) {&#10;        viewModelScope.launch {&#10;            try {&#10;                val entityCategory = when (category) {&#10;                    InvestmentCategory.STOCK -&gt; EntityCategory.STOCK&#10;                    InvestmentCategory.OTHER -&gt; EntityCategory.OTHER&#10;                }&#10;&#10;                val existing = investmentDao.findByDescriptionAndCategory(description, entityCategory)&#10;&#10;                if (existing != null) {&#10;                    // 合并到已有条目&#10;                    val updated = existing.copy(&#10;                        quantity = existing.quantity + quantity,&#10;                        investment = existing.investment + investment,&#10;                        currentValue = if (category == InvestmentCategory.OTHER &amp;&amp; currentValue != null) {&#10;                            existing.currentValue + currentValue&#10;                        } else {&#10;                            existing.currentValue&#10;                        },&#10;                        updatedAt = System.currentTimeMillis()&#10;                    )&#10;                    investmentDao.update(updated)&#10;                    sendEvent(InvestmentUiEvent.ShowMessage(&quot;已合并到现有条目&quot;))&#10;                } else {&#10;                    // 新建条目&#10;                    val entity = InvestmentEntity(&#10;                        category = entityCategory,&#10;                        description = description,&#10;                        quantity = quantity,&#10;                        investment = investment,&#10;                        currentPrice = 0.0,&#10;                        currentValue = currentValue ?: investment&#10;                    )&#10;                    investmentDao.insert(entity)&#10;                    sendEvent(InvestmentUiEvent.ShowMessage(&quot;添加成功&quot;))&#10;                }&#10;&#10;                hideAddDialog()&#10;                sendEvent(InvestmentUiEvent.InvestmentAdded)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;添加失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 更新投资条目。&#10;     * 股票类保留原有 currentPrice（由行情刷新维护），仅更新描述、数量、成本；&#10;     * 其他类则用传入的 currentValue 直接覆盖。&#10;     * @param id           条目 ID&#10;     * @param category     品类&#10;     * @param description  描述&#10;     * @param quantity     数量&#10;     * @param investment   投入成本&#10;     * @param currentValue 当前市值（非股票类可选）&#10;     */&#10;    fun updateInvestment(&#10;        id: Long,&#10;        category: InvestmentCategory,&#10;        description: String,&#10;        quantity: Double,&#10;        investment: Double,&#10;        currentValue: Double? = null&#10;    ) {&#10;        viewModelScope.launch {&#10;            try {&#10;                val existing = investmentDao.getById(id) ?: return@launch&#10;&#10;                val entityCategory = when (category) {&#10;                    InvestmentCategory.STOCK -&gt; EntityCategory.STOCK&#10;                    InvestmentCategory.OTHER -&gt; EntityCategory.OTHER&#10;                }&#10;&#10;                val updated = existing.copy(&#10;                    category = entityCategory,&#10;                    description = description,&#10;                    quantity = quantity,&#10;                    investment = investment,&#10;                    currentPrice = if (category == InvestmentCategory.STOCK) existing.currentPrice else 0.0,&#10;                    currentValue = if (category == InvestmentCategory.OTHER) {&#10;                        currentValue ?: investment&#10;                    } else {&#10;                        existing.currentValue  // 股票类保持原市值&#10;                    },&#10;                    updatedAt = System.currentTimeMillis()&#10;                )&#10;&#10;                investmentDao.update(updated)&#10;                hideAddDialog()&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;更新成功&quot;))&#10;                sendEvent(InvestmentUiEvent.InvestmentUpdated)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;更新失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 按 ID 删除投资条目，并发送成功与删除事件。&#10;     * @param investment 待删除的投资领域对象&#10;     */&#10;    fun deleteInvestment(investment: Investment) {&#10;        viewModelScope.launch {&#10;            try {&#10;                investmentDao.deleteById(investment.id)&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;删除成功&quot;))&#10;                sendEvent(InvestmentUiEvent.InvestmentDeleted)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;删除失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /** InvestmentEntity → Investment 领域模型映射 */&#10;    private fun toDomainModel(entity: InvestmentEntity): Investment {&#10;        return Investment(&#10;            id = entity.id,&#10;            category = when (entity.category) {&#10;                EntityCategory.STOCK -&gt; InvestmentCategory.STOCK&#10;                EntityCategory.OTHER -&gt; InvestmentCategory.OTHER&#10;            },&#10;            description = entity.description,&#10;            quantity = entity.quantity,&#10;            investment = entity.investment,&#10;            currentPrice = entity.currentPrice,&#10;            currentValue = entity.currentValue,&#10;            createdAt = entity.createdAt,&#10;            updatedAt = entity.updatedAt&#10;        )&#10;    }&#10;&#10;    /**&#10;     * 启动周期性股票价格刷新 Job。&#10;     * 先立即执行一次，之后每 60 秒循环刷新，&#10;     * 内部刷新结果（成功/失败）不做 UI 提示，仅做后台静默更新。&#10;     */&#10;    private fun startStockPriceRefresh() {&#10;        stockPriceRefreshJob?.cancel()&#10;        stockPriceRefreshJob = viewModelScope.launch {&#10;            refreshStockPricesInternal()&#10;&#10;            while (isActive) {&#10;                delay(60_000)&#10;                refreshStockPricesInternal()&#10;            }&#10;        }&#10;    }&#10;&#10;    /** 后台静默刷新股票价格，不向用户反馈结果 */&#10;    private suspend fun refreshStockPricesInternal() {&#10;        when (val result = investmentRepository.refreshAllStockPrices()) {&#10;            is Resource.Success -&gt; {}&#10;            is Resource.Error -&gt; {}&#10;            is Resource.Loading -&gt; {}&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 用户手动触发股票价格刷新。&#10;     * 设置刷新中的 UI 状态，完成后根据结果发送成功/失败消息。&#10;     */&#10;    fun refreshStockPrices() {&#10;        viewModelScope.launch {&#10;            updateState { copy(isRefreshing = true) }&#10;            when (val result = investmentRepository.refreshAllStockPrices()) {&#10;                is Resource.Success -&gt; sendEvent(InvestmentUiEvent.ShowMessage(&quot;刷新成功&quot;))&#10;                is Resource.Error -&gt; sendEvent(InvestmentUiEvent.ShowMessage(&quot;刷新失败: ${result.message}&quot;))&#10;                is Resource.Loading -&gt; {}&#10;            }&#10;            updateState { copy(isRefreshing = false) }&#10;        }&#10;    }&#10;&#10;    /** ViewModel 销毁时取消行情刷新 Job，防止协程泄漏 */&#10;    override fun onCleared() {&#10;        super.onCleared()&#10;        stockPriceRefreshJob?.cancel()&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该仓库负责查询股票代码、调用行情接口并把最新价格写回数据库，逻辑重点是先查本地持仓，再批量刷新价格。&#10;@Singleton&#10;class InvestmentRepositoryImpl @Inject constructor(&#10;    private val investmentDao: InvestmentDao,&#10;    private val stockApiService: StockApiService&#10;) : InvestmentRepository {&#10;&#10;    companion object {&#10;        private const val TAG = &quot;InvestmentRepository&quot;&#10;    }&#10;&#10;    // 常规 CRUD 与映射方法省略，保留行情刷新核心流程&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 批量刷新全部持仓股票的最新价格。&#10;     * 流程：查询本地持仓代码 → 转换为行情接口所需格式 → 批量请求 → 解析响应 →&#10;     * 按原始代码回写 currentPrice 至数据库。&#10;     * 若持仓为空则直接返回成功；若响应中无有效行情数据则返回 Error。&#10;     * @return Resource&lt;Unit&gt; 成功时携带 Unit，失败时携带错误描述&#10;     */&#10;    override suspend fun refreshAllStockPrices(): Resource&lt;Unit&gt; {&#10;        return try {&#10;            // 读取本地持仓代码，作为后端行情代理的输入&#10;            val stockCodes = investmentDao.getAllStockCodes()&#10;            Log.d(TAG, &quot;获取到的股票代码: $stockCodes&quot;)&#10;&#10;            if (stockCodes.isEmpty()) {&#10;                Log.d(TAG, &quot;没有股票需要刷新价格&quot;)&#10;                return Resource.Success(Unit)&#10;            }&#10;&#10;            // 统一代码格式后批量请求，减少网络往返&#10;            val sinaSymbols = stockCodes.map { convertToSinaSymbol(it) }&#10;            val symbolsParam = sinaSymbols.joinToString(&quot;,&quot;)&#10;            Log.d(TAG, &quot;请求后端股票行情 API, symbols: $symbolsParam&quot;)&#10;&#10;            val response = stockApiService.getBatchQuotes(BatchQuoteRequest(sinaSymbols))&#10;            Log.d(TAG, &quot;API响应码: ${response.code()}, 是否成功: ${response.isSuccessful}&quot;)&#10;&#10;            if (response.isSuccessful &amp;&amp; response.body() != null) {&#10;                val quotes = response.body()!!.data?.quotes.orEmpty()&#10;                Log.d(TAG, &quot;解析到 ${quotes.size} 个股票价格&quot;)&#10;&#10;                if (quotes.isEmpty()) {&#10;                    Log.w(TAG, &quot;没有获取到有效的股票价格数据&quot;)&#10;                    return Resource.Error(&quot;未找到股票数据，请检查股票代码格式&quot;)&#10;                }&#10;&#10;                // 逐个回写数据库，按原始代码匹配确保一致性&#10;                quotes.forEach { result -&gt;&#10;                    Log.d(TAG, &quot;更新股票 ${result.symbol} 价格: ${result.currentPrice ?: 0.0}&quot;)&#10;                    val originalCode = findOriginalCode(stockCodes, result.symbol)&#10;                    if (originalCode != null) {&#10;                        // 按原始代码回写数据库，确保界面列表与本地数据一致&#10;                        investmentDao.updateStockPrice(originalCode, result.currentPrice ?: 0.0)&#10;                    }&#10;                }&#10;&#10;                Resource.Success(Unit)&#10;            } else {&#10;                val errorBody = response.errorBody()?.string()&#10;                Log.e(TAG, &quot;API请求失败: ${response.code()}, 错误信息: $errorBody&quot;)&#10;                Resource.Error(&quot;获取股票价格失败: ${response.code()} - ${response.message()}&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            Log.e(TAG, &quot;刷新股票价格异常&quot;, e)&#10;            Resource.Error(&quot;网络错误: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 将本地股票代码转换为行情接口所需的 Symbol 格式。&#10;     * 规则：&#10;     *   6 位数字且首位为 6 → sh 前缀（沪市）；&#10;     *   6 位数字且首位为 0 或 3 → sz 前缀（深市）；&#10;     *   5 位数字 → hk 前缀（港股）；&#10;     *   已含 SH/SZ/HK/GB_ 前缀 → 转小写保留；&#10;     *   其他 → gb_ 前缀（美股等）。&#10;     * @param code 本地存储的股票代码&#10;     * @return 行情接口格式的 Symbol 字符串&#10;     */&#10;    private fun convertToSinaSymbol(code: String): String {&#10;        val upperCode = code.uppercase().trim()&#10;&#10;        return when {&#10;            upperCode.startsWith(&quot;SH&quot;) || upperCode.startsWith(&quot;SZ&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.startsWith(&quot;HK&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.startsWith(&quot;GB_&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.matches(Regex(&quot;^6\\d{5}$&quot;)) -&gt; &quot;sh$upperCode&quot;&#10;            upperCode.matches(Regex(&quot;^[03]\\d{5}$&quot;)) -&gt; &quot;sz$upperCode&quot;&#10;            upperCode.matches(Regex(&quot;^\\d{5}$&quot;)) -&gt; &quot;hk$upperCode&quot;&#10;            else -&gt; &quot;gb_${upperCode.lowercase()}&quot;&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 反向查找原始代码。行情接口返回的 Symbol 格式与本地存储格式不同，&#10;     * 需将返回的 Symbol 与本地代码逐一比对（均转换为统一格式后比较），&#10;     * 以找到对应原始代码进行数据库回写。&#10;     * @param originalCodes 本地存储的股票代码列表&#10;     * @param sinaSymbol    行情接口返回的 Symbol&#10;     * @return 匹配的原始代码，若未匹配则返回 null&#10;     */&#10;    private fun findOriginalCode(originalCodes: List&lt;String&gt;, sinaSymbol: String): String? {&#10;        val sinaUpper = sinaSymbol.uppercase()&#10;&#10;        for (code in originalCodes) {&#10;            val converted = convertToSinaSymbol(code).uppercase()&#10;            if (converted == sinaUpper) {&#10;                return code&#10;            }&#10;        }&#10;&#10;        return null&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;}&#10;```&#10;&#10;该模块体现了本地优先展示与异步刷新的实现思路，使投资数据能够在不阻塞界面的前提下保持较高的时效性。&#10;&#10;5.1.5 数据管理模块实现&#10;&#10;数据管理模块主要实现本地持久化结构定义、DAO 接口设计、Repository 实现以及跨端数据同步管理。实现策略以离线优先为核心，所有实体均包含 `serverId`、`syncStatus` 与 `lastSyncAt` 等字段，以便在网络恢复后执行增量同步。Schema 演化采用 Room 的迁移机制，复杂迁移脚本需在构建阶段或迁移工具中提前验证。&#10;&#10;在实现层面，DAO 保持细粒度查询方法并返回 Flow，与 ViewModel 和 UseCase 层自然衔接。RepositoryImpl 遵循本地优先、网络回补的策略，`SyncManager` 则负责消费待同步队列并与后端保持幂等交互。下面的两段代码分别展示 `TransactionDao` 的查询与同步状态接口，以及 `SyncManager` 的交易同步入口。&#10;&#10;```kotlin&#10;// 说明：该 DAO 提供交易查询、增删改和同步状态维护接口，整体逻辑是把本地数据与同步队列分开管理。&#10;@Dao&#10;interface TransactionDao {&#10;    /**&#10;     * 按日期范围查询交易记录，以 Flow 形式返回并按日期降序排列，&#10;     * 供界面层订阅以获取实时更新的流水数据。&#10;     */&#10;    @Query(&quot;SELECT * FROM transactions WHERE date BETWEEN :startDate AND :endDate ORDER BY date DESC&quot;)&#10;    fun getTransactionsByDateRange(startDate: Long, endDate: Long): Flow&lt;List&lt;TransactionEntity&gt;&gt;&#10;&#10;    /**&#10;     * 查询所有待同步交易（默认状态：PENDING_UPLOAD 和 PENDING_DELETE），&#10;     * 由 SyncManager 批量消费。&#10;     */&#10;    @Query(&quot;SELECT * FROM transactions WHERE syncStatus IN (:statuses)&quot;)&#10;    suspend fun getPendingSyncTransactions(statuses: List&lt;SyncStatus&gt; = listOf(SyncStatus.PENDING_UPLOAD, SyncStatus.PENDING_DELETE)): List&lt;TransactionEntity&gt;&#10;&#10;    /**&#10;     * 更新指定交易的同步状态与最后同步时间，&#10;     * 通常在成功同步或冲突解决后调用。&#10;     */&#10;    @Query(&quot;UPDATE transactions SET syncStatus = :status, lastSyncAt = :syncTime WHERE id = :id&quot;)&#10;    suspend fun updateSyncStatus(id: Long, status: SyncStatus, syncTime: Long = System.currentTimeMillis())&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该同步管理器负责统一处理交易与账户的上传、删除和冲突解决，核心逻辑是先检查网络与会话，再批量同步本地待处理数据。&#10;@Singleton&#10;class SyncManager @Inject constructor(&#10;    private val transactionDao: TransactionDao,&#10;    private val accountDao: AccountDao,&#10;    private val syncMetadataDao: SyncMetadataDao,&#10;    private val expenseApiService: ExpenseApiService,&#10;    private val accountApiService: AccountApiService,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) {&#10;    /** 同步状态流，ViewModel 层订阅以展示同步进度 */&#10;    private val _syncState = MutableStateFlow&lt;SyncState&gt;(SyncState.Idle)&#10;    val syncState: StateFlow&lt;SyncState&gt; = _syncState.asStateFlow()&#10;&#10;    /** 待同步条目计数流，供界面展示离线积压数量 */&#10;    private val _pendingSyncCount = MutableStateFlow(0)&#10;    val pendingSyncCount: StateFlow&lt;Int&gt; = _pendingSyncCount.asStateFlow()&#10;&#10;    companion object {&#10;        const val TABLE_TRANSACTIONS = &quot;transactions&quot;&#10;        const val TABLE_ACCOUNTS = &quot;accounts&quot;&#10;    }&#10;&#10;    init {&#10;        observeNetworkAndSync()     // 监听网络恢复，自动触发同步&#10;        updatePendingSyncCount()    // 初始化待同步计数&#10;    }&#10;&#10;    /**&#10;     * 监听网络状态变化。当网络恢复且存在待同步条目且用户已登录时，自动触发全量同步，&#10;     * 实现离线操作后网络恢复时的透明数据上传。&#10;     */&#10;    private fun observeNetworkAndSync() {&#10;        scope.launch {&#10;            networkMonitor.observeNetworkStatus()&#10;                .filter { it == NetworkStatus.AVAILABLE }  // 仅在网络可用时触发&#10;                .collect {&#10;                    if (_pendingSyncCount.value &gt; 0 &amp;&amp; userPreferencesManager.hasBackendSession()) {&#10;                        syncAll()&#10;                    }&#10;                }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 重新计算待同步条目总数（交易 + 账户），更新 _pendingSyncCount。&#10;     * 在各同步操作完成后调用以反映最新积压状态。&#10;     */&#10;    private fun updatePendingSyncCount() {&#10;        scope.launch {&#10;            val transactionCount = transactionDao.getPendingSyncCount()&#10;            val accountCount = accountDao.getPendingSyncCount()&#10;            _pendingSyncCount.value = transactionCount + accountCount&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 全量同步入口。&#10;     * 前置校验：检查用户登录状态与网络可用性；&#10;     * 执行流程：依次同步交易与账户，累加成功计数，最后更新待同步计数与同步状态。&#10;     * @return SyncResult&lt;Int&gt; 包含成功同步的总条目数或错误信息&#10;     */&#10;    fun syncAll(): SyncResult&lt;Int&gt; {&#10;        if (!userPreferencesManager.hasBackendSession()) {&#10;            return SyncResult.Error(&quot;未登录，数据已保存在本地，登录后将自动同步&quot;)&#10;        }&#10;&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用，数据已保存在本地&quot;)&#10;        }&#10;&#10;        _syncState.value = SyncState.Syncing&#10;        var totalSynced = 0&#10;&#10;        return try {&#10;            // 先同步交易，再同步账户&#10;            val transactionResult = syncTransactions()&#10;            if (transactionResult is SyncResult.Success) {&#10;                totalSynced += transactionResult.data&#10;            }&#10;&#10;            val accountResult = syncAccounts()&#10;            if (accountResult is SyncResult.Success) {&#10;                totalSynced += accountResult.data&#10;            }&#10;&#10;            _syncState.value = SyncState.Success(totalSynced)&#10;            updatePendingSyncCount()&#10;            SyncResult.Success(totalSynced)&#10;        } catch (e: Exception) {&#10;            val errorMessage = &quot;同步失败: ${e.message}&quot;&#10;            _syncState.value = SyncState.Error(errorMessage, e)&#10;            SyncResult.Error(errorMessage, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 同步交易记录：将本地 PENDING_UPLOAD 条目批量上传，处理 PENDING_DELETE 条目，&#10;     * 并应对 HTTP 409 冲突。&#10;     * 流程：查询待同步列表 → 分离上传/删除 → 上传 → 处理响应（200/409/其他） → 删除本地条目。&#10;     * @return SyncResult&lt;Int&gt; 成功时携带已同步条目数&#10;     */&#10;    fun syncTransactions(): SyncResult&lt;Int&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val pendingTransactions = transactionDao.getPendingSyncTransactions()&#10;            if (pendingTransactions.isEmpty()) {&#10;                return SyncResult.Success(0)&#10;            }&#10;&#10;            // 按同步状态分流&#10;            val toUpload = pendingTransactions.filter { it.syncStatus == SyncStatus.PENDING_UPLOAD }&#10;            val toDelete = pendingTransactions.filter { it.syncStatus == SyncStatus.PENDING_DELETE }&#10;&#10;            var syncedCount = 0&#10;&#10;            // ── 上传待同步条目 ──&#10;            if (toUpload.isNotEmpty()) {&#10;                val dtos = toUpload.map { it.toSyncDto() }&#10;                val lastSyncTime = syncMetadataDao.getByTableName(TABLE_TRANSACTIONS)?.lastSyncTimestamp ?: 0&#10;                val request = SyncRequest(dtos, lastSyncTime)&#10;&#10;                val response = expenseApiService.syncTransactions(request)&#10;                if (response.isSuccessful) {&#10;                    val body = response.body()&#10;                    if (body?.code == 200) {&#10;                        val serverTransactions = body.data.orEmpty()&#10;&#10;                        // 将服务端返回的 ID 回写本地，建立映射&#10;                        for (entity in toUpload) {&#10;                            val match = findServerTransactionForLocal(entity, serverTransactions)&#10;                            val serverId = match?.id?.takeIf { it &gt; 0 }&#10;                            if (serverId != null) {&#10;                                transactionDao.updateServerId(entity.id, serverId.toString(), SyncStatus.SYNCED)&#10;                            } else {&#10;                                transactionDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                            }&#10;                        }&#10;                        syncedCount += toUpload.size&#10;                    } else {&#10;                        return SyncResult.Error(&quot;同步交易失败: ${body?.message}&quot;)&#10;                    }&#10;                } else if (response.code() == 409) {&#10;                    // HTTP 409 表示冲突，触发冲突解决流程&#10;                    return handleTransactionConflict(toUpload)&#10;                } else {&#10;                    return SyncResult.Error(&quot;服务器返回错误: ${response.code()}&quot;)&#10;                }&#10;            }&#10;&#10;            // ── 处理待删除条目 ──&#10;            if (toDelete.isNotEmpty()) {&#10;                for (entity in toDelete) {&#10;                    entity.serverId?.toLongOrNull()?.let { serverId -&gt;&#10;                        val response = expenseApiService.deleteTransaction(serverId)&#10;                        if (response.isSuccessful) {&#10;                            transactionDao.delete(entity)&#10;                            syncedCount++&#10;                        }&#10;                    } ?: run {&#10;                        // 无 serverId 则直接删除（本地未上云数据）&#10;                        transactionDao.delete(entity)&#10;                        syncedCount++&#10;                    }&#10;                }&#10;            }&#10;&#10;            updateSyncMetadata(TABLE_TRANSACTIONS)&#10;            SyncResult.Success(syncedCount)&#10;        } catch (e: Exception) {&#10;            updateSyncError(TABLE_TRANSACTIONS, e.message)&#10;            SyncResult.Error(&quot;同步交易记录失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 处理交易同步冲突（HTTP 409）。&#10;     * 拉取服务端最新数据，以 updatedAt 时间戳判断冲突方向：&#10;     * 若服务端版本更新或本地处于 CONFLICT 状态，则用服务端数据覆盖本地；&#10;     * 完成后将本地条目标记为 SYNCED。&#10;     * @param toUpload 触发冲突的本地待上传条目&#10;     * @return SyncResult&lt;Int&gt; 携带成功解决的条目数&#10;     */&#10;    private fun handleTransactionConflict(toUpload: List&lt;TransactionEntity&gt;): SyncResult&lt;Int&gt; {&#10;        return try {&#10;            val now = System.currentTimeMillis()&#10;            val response = expenseApiService.getTransactions(0, now)&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverTransactions = response.body()?.data ?: emptyList()&#10;                for (serverTx in serverTransactions) {&#10;                    serverTx.id?.let { serverId -&gt;&#10;                        val localEntity = transactionDao.getByServerId(serverId.toString())&#10;                        if (localEntity != null) {&#10;                            // 冲突解决策略：以服务端时间戳为准，更新本地版本&#10;                            if (serverTx.updatedAt != null &amp;&amp;&#10;                                (localEntity.updatedAt &lt; serverTx.updatedAt || localEntity.syncStatus == SyncStatus.CONFLICT)&#10;                            ) {&#10;                                transactionDao.update(serverTx.toEntity().copy(id = localEntity.id))&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;                for (entity in toUpload) {&#10;                    transactionDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                }&#10;                updateSyncMetadata(TABLE_TRANSACTIONS)&#10;                SyncResult.Success(toUpload.size)&#10;            } else {&#10;                SyncResult.Error(&quot;冲突处理失败&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;冲突处理失败: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 同步账户数据。逻辑与 syncTransactions 对称：&#10;     * 上传 PENDING_UPLOAD 条目 → 处理 409 冲突 → 删除 PENDING_DELETE 条目。&#10;     * @return SyncResult&lt;Int&gt; 成功时携带已同步条目数&#10;     */&#10;    fun syncAccounts(): SyncResult&lt;Int&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val pendingAccounts = accountDao.getPendingSyncAccounts()&#10;            if (pendingAccounts.isEmpty()) {&#10;                return SyncResult.Success(0)&#10;            }&#10;&#10;            val toUpload = pendingAccounts.filter { it.syncStatus == SyncStatus.PENDING_UPLOAD }&#10;            val toDelete = pendingAccounts.filter { it.syncStatus == SyncStatus.PENDING_DELETE }&#10;&#10;            var syncedCount = 0&#10;&#10;            if (toUpload.isNotEmpty()) {&#10;                val dtos = toUpload.map { it.toSyncDto() }&#10;                val lastSyncTime = syncMetadataDao.getByTableName(TABLE_ACCOUNTS)?.lastSyncTimestamp ?: 0&#10;                val request = AccountSyncRequest(dtos, lastSyncTime)&#10;&#10;                val response = accountApiService.syncAccounts(request)&#10;                if (response.isSuccessful) {&#10;                    val body = response.body()&#10;                    if (body?.code == 200) {&#10;                        val serverAccounts = body.data ?: emptyList()&#10;&#10;                        for (entity in toUpload) {&#10;                            val match = findServerAccountForLocal(entity, serverAccounts)&#10;                            val serverId = match?.id?.takeIf { it &gt; 0 }&#10;                            if (serverId != null) {&#10;                                accountDao.updateServerId(entity.id, serverId.toString(), SyncStatus.SYNCED)&#10;                            } else {&#10;                                accountDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                            }&#10;                        }&#10;                        syncedCount += toUpload.size&#10;                    } else {&#10;                        return SyncResult.Error(&quot;同步账户失败: ${body?.message}&quot;)&#10;                    }&#10;                } else if (response.code() == 409) {&#10;                    return handleAccountConflict(toUpload)&#10;                } else {&#10;                    return SyncResult.Error(&quot;服务器返回错误: ${response.code()}&quot;)&#10;                }&#10;            }&#10;&#10;            if (toDelete.isNotEmpty()) {&#10;                for (entity in toDelete) {&#10;                    entity.serverId?.toLongOrNull()?.let { serverId -&gt;&#10;                        val response = accountApiService.deleteAccount(serverId)&#10;                        if (response.isSuccessful) {&#10;                            accountDao.delete(entity)&#10;                            syncedCount++&#10;                        }&#10;                    } ?: run {&#10;                        accountDao.delete(entity)&#10;                        syncedCount++&#10;                    }&#10;                }&#10;            }&#10;&#10;            updateSyncMetadata(TABLE_ACCOUNTS)&#10;            SyncResult.Success(syncedCount)&#10;        } catch (e: Exception) {&#10;            updateSyncError(TABLE_ACCOUNTS, e.message)&#10;            SyncResult.Error(&quot;同步账户数据失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 处理账户同步冲突。&#10;     * 与服务端版本比较 updatedAt，若服务端版本更新则覆盖本地数据。&#10;     * @param toUpload 触发冲突的本地待上传账户列表&#10;     * @return SyncResult&lt;Int&gt; 携带成功解决的条目数&#10;     */&#10;    private fun handleAccountConflict(toUpload: List&lt;AccountEntity&gt;): SyncResult&lt;Int&gt; {&#10;        return try {&#10;            val response = accountApiService.getAccounts()&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverAccounts = response.body()?.data ?: emptyList()&#10;                for (serverAcct in serverAccounts) {&#10;                    serverAcct.id?.let { serverId -&gt;&#10;                        val localEntity = accountDao.getByServerId(serverId.toString())&#10;                        if (localEntity != null) {&#10;                            if (serverAcct.updatedAt != null &amp;&amp; localEntity.updatedAt &lt; serverAcct.updatedAt) {&#10;                                accountDao.update(serverAcct.toEntity().copy(id = localEntity.id))&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;                for (entity in toUpload) {&#10;                    accountDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                }&#10;                updateSyncMetadata(TABLE_ACCOUNTS)&#10;                SyncResult.Success(toUpload.size)&#10;            } else {&#10;                SyncResult.Error(&quot;账户冲突处理失败&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;账户冲突处理失败: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 从服务端拉取指定时间范围内的交易记录并合并到本地。&#10;     * 对于已存在于本地的记录（以 serverId 匹配），若非本地待上传状态则更新；&#10;     * 未匹配到的记录直接插入本地数据库。&#10;     * @param startDate 查询起始时间戳&#10;     * @param endDate   查询结束时间戳&#10;     * @return SyncResult&lt;List&lt;TransactionEntity&gt;&gt; 携带拉取的实体列表&#10;     */&#10;    fun pullTransactionsFromServer(startDate: Long, endDate: Long): SyncResult&lt;List&lt;TransactionEntity&gt;&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val response = expenseApiService.getTransactions(startDate, endDate)&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverTransactions = response.body()?.data ?: emptyList()&#10;                val entities = serverTransactions.map { it.toEntity() }&#10;&#10;                for (entity in entities) {&#10;                    val sid = entity.serverId&#10;                    if (sid != null) {&#10;                        val localEntity = transactionDao.getByServerId(sid)&#10;                        if (localEntity != null) {&#10;                            // 保护本地待上传数据不被覆盖&#10;                            if (localEntity.syncStatus != SyncStatus.PENDING_UPLOAD) {&#10;                                transactionDao.update(entity.copy(id = localEntity.id))&#10;                            }&#10;                        } else {&#10;                            transactionDao.insert(entity)&#10;                        }&#10;                    }&#10;                }&#10;&#10;                SyncResult.Success(entities)&#10;            } else {&#10;                SyncResult.Error(&quot;服务器返回错误: ${response.body()?.message}&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;拉取数据失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /** 更新指定表的同步元数据时间戳，记录最后一次成功同步时间 */&#10;    private fun updateSyncMetadata(tableName: String) { /* omitted in doc excerpt */ }&#10;    /** 记录指定表同步过程中的错误信息 */&#10;    private fun updateSyncError(tableName: String, error: String?) { /* omitted in doc excerpt */ }&#10;&#10;    /** 是否存在待同步数据 */&#10;    fun needsSync(): Boolean {&#10;        return _pendingSyncCount.value &gt; 0&#10;    }&#10;&#10;    /** 手动刷新待同步计数 */&#10;    fun refreshPendingCount() {&#10;        updatePendingSyncCount()&#10;    }&#10;&#10;    /**&#10;     * 在服务端返回列表中匹配本地交易实体。&#10;     * 匹配策略：选取 createdAt 时间差最小的服务端记录。&#10;     */&#10;    private fun findServerTransactionForLocal(local: TransactionEntity, serverList: List&lt;TransactionDto&gt;): TransactionDto? { return serverList.minByOrNull { kotlin.math.abs((it.createdAt ?: it.date) - local.createdAt) }&#10;    }&#10;&#10;    /**&#10;     * 在服务端返回列表中匹配本地账户实体，&#10;     * 以 createdAt 最小绝对差作为匹配依据。&#10;     */&#10;    private fun findServerAccountForLocal(local: AccountEntity, serverList: List&lt;AccountDto&gt;): AccountDto? { return serverList.minByOrNull { kotlin.math.abs((it.createdAt ?: 0L) - local.createdAt) }&#10;    }&#10;&#10;    /** TransactionEntity → TransactionDto 映射，用于上传同步 */&#10;    private fun toSyncDto(entity: TransactionEntity): TransactionDto { return TransactionDto(id = entity.serverId?.toLongOrNull() ?: 0L, amount = entity.amount, type = entity.type.name, category = entity.category, note = entity.note, date = entity.date, accountId = entity.accountId.takeIf { it &gt; 0 }, createdAt = entity.createdAt, updatedAt = entity.updatedAt, deletedAt = null) }&#10;&#10;    /** TransactionDto → TransactionEntity 映射，用于服务端数据拉取后的本地转换 */&#10;    private fun toEntity(dto: TransactionDto): TransactionEntity { return TransactionEntity(serverId = dto.id?.takeIf { it &gt; 0 }?.toString(), amount = dto.amount, type = try { TransactionType.valueOf(dto.type) } catch (_: Exception) { TransactionType.EXPENSE }, category = dto.category, accountId = dto.accountId ?: 0, note = dto.note, date = dto.date, createdAt = dto.createdAt ?: dto.date, updatedAt = dto.updatedAt ?: System.currentTimeMillis(), syncStatus = SyncStatus.SYNCED, lastSyncAt = System.currentTimeMillis()) }&#10;&#10;    /** AccountEntity → AccountDto 映射 */&#10;    private fun toSyncDto(entity: AccountEntity): AccountDto { return AccountDto(id = entity.serverId?.toLongOrNull() ?: 0L, name = entity.name, icon = entity.icon, balance = entity.balance, isDefault = entity.isDefault, sortOrder = entity.sortOrder, createdAt = entity.createdAt, updatedAt = entity.updatedAt) }&#10;&#10;    /** AccountDto → AccountEntity 映射 */&#10;    private fun toEntity(dto: AccountDto): AccountEntity { return AccountEntity(serverId = dto.id?.takeIf { it &gt; 0 }?.toString(), name = dto.name, icon = dto.icon, balance = dto.balance, isDefault = dto.isDefault, sortOrder = dto.sortOrder, createdAt = dto.createdAt ?: System.currentTimeMillis(), updatedAt = dto.updatedAt ?: System.currentTimeMillis(), syncStatus = SyncStatus.SYNCED, lastSyncAt = System.currentTimeMillis()) }&#10;}&#10;```&#10;```&#10;&#10;上述实现保证了数据层在离线场景、并发修改与网络不稳定条件下仍可维持一致性与可恢复性，也为后端的最终合并与冲突处理提供了基础。&#10;&#10;参考与说明&#10;文中示例代码均为摘录性质，目的在于说明各模块的核心职责与实现思路。整体示例遵循仓库中既有的 MVVM 与 Clean Architecture 约定，相关符号与文件路径可参照仓库源码，例如 `app/src/main/java/com/example/funnyexpensetracking/ui/transaction/TransactionViewModel.kt`、`domain/usecase/RealtimeAssetCalculator.kt` 以及 `app/src/main/java/com/example/funnyexpensetracking/config/ApiEnvironmentConfig.kt`。如需进一步扩展为可直接编译的完整类，可继续补充上下文与依赖注入细节。&#10;&#10;" />
              <option name="updatedContent" value="身份设定&#10;你是专注于中文学术论文优化的资深编辑，主要职责是将机械化的 AI 生成文本调整为符合期刊规范的学术表述，在保持原文研究内容、技术逻辑与论证链条完整的前提下，让行文更自然，也更便于阅读。&#10;&#10;操作准则&#10;1. 不改变原文的研究假设、实验数据与核心结论等关键信息；&#10;2. 避免使用不符合目标期刊领域的口语化或非专业表达；&#10;3. 不引入与原文无关的额外观点或数据；&#10;4. 不弱化也不过度扩展原有的逻辑论证关系；&#10;5. 不使用夸张或生僻的学术词汇；&#10;6. 减少机械化连接词，让段落衔接更顺一些，但仍保持论文语气；&#10;7. 控制括号使用频率，优先采用逗号、句号和从句结构组织信息。&#10;&#10;改写执行步骤&#10;1. 机械内容改写：针对 AI 生成痕迹进行逐项修订，使文本更接近期刊常见表述；&#10;2. 整体逻辑优化：结合章节结构调整段落顺序与句间衔接方式，让论述更顺畅；&#10;3. 专业术语校验：核对改写后的术语使用是否与原文保持一致，确保技术表述准确。&#10;&#10;代码核心片段截取规则&#10;1. 对于整个类的截取，非关键方法可使用 `/* logic omitted for brevity */` 进行省略；&#10;2. 对于核心逻辑代码的截取，需完整呈现，并在关键分支和数据流位置附带注释说明。&#10;&#10;5.1 Android 客户端功能模块实现&#10;&#10;5.1.1 登录模块实现&#10;&#10;本模块主要实现用户身份验证与会话建立。系统在移动端采用离线优先与渐进同步的实现思路，先完成输入合法性校验，再发起远端认证请求，随后依据返回结果完成本地会话保存及后续同步处理。这样的设计既能保持较快的交互响应，也便于在登录后尽快恢复用户的本地状态。&#10;&#10;从架构层面看，登录功能位于 UI 层与数据层之间，由 ViewModel 负责输入校验、状态维护与事件分发，Repository 负责网络调用及本地持久化，环境切换则由统一配置类管理，相关实现可参见 `app/src/main/java/com/example/funnyexpensetracking/config/ApiEnvironmentConfig.kt`。&#10;&#10;在 ViewModel 层中，`LoginViewModel` 负责输入校验、状态切换以及登录成功后的同步触发，核心代码如上所示。Repository 层中，`AuthRepository` 定义认证契约，`AuthRepositoryImpl` 负责向后端发起登录请求并保存会话信息，完成令牌写入、用户信息持久化和退出登录后的会话清理。登录流程由此形成“校验、认证、持久化”的完整链路。&#10;&#10;```kotlin&#10;/**&#10; * 登录界面状态密封接口，定义四种互斥的界面状态。&#10; * Idle: 初始空闲状态；Loading: 认证请求进行中；&#10; * Success: 登录成功，即将触发数据同步；Error: 认证失败并携带提示信息。&#10; */&#10;sealed interface LoginUiState {&#10;    data object Idle : LoginUiState            // 表单初始状态，等待用户输入&#10;    data object Loading : LoginUiState         // 认证请求已发出，界面展示加载指示器&#10;    data object Success : LoginUiState         // 认证成功，即将跳转主界面并触发同步&#10;    data class Error(val message: String) : LoginUiState  // 认证失败，携带本地化错误信息&#10;}&#10;&#10;@HiltViewModel&#10;class LoginViewModel @Inject constructor(&#10;    private val authRepository: AuthRepository,&#10;    private val syncManager: com.example.funnyexpensetracking.data.sync.SyncManager&#10;) : ViewModel() {&#10;&#10;    private val _uiState = MutableStateFlow&lt;LoginUiState&gt;(LoginUiState.Idle)&#10;    val uiState: StateFlow&lt;LoginUiState&gt; = _uiState.asStateFlow()&#10;&#10;    /**&#10;     * 执行登录认证流程。&#10;     * 输入校验 → Loading 状态 → 远端认证 → Success/Error 状态。&#10;     * 成功时触发全量同步以恢复用户数据，失败时传递异常消息至 UI 层。&#10;     * @param email    用户邮箱，内部自动 trim&#10;     * @param password 明文密码&#10;     */&#10;    fun login(email: String, password: String) {&#10;        // 空输入提前拦截，避免无效网络请求&#10;        if (email.isBlank() || password.isBlank()) {&#10;            _uiState.value = LoginUiState.Error(&quot;请输入邮箱和密码&quot;)&#10;            return&#10;        }&#10;        viewModelScope.launch {&#10;            _uiState.value = LoginUiState.Loading            // 进入加载态&#10;            authRepository.login(email.trim(), password)&#10;                .onSuccess {&#10;                    _uiState.value = LoginUiState.Success     // 认证通过&#10;                    viewModelScope.launch { syncManager.syncAll() }  // 立即同步离线数据&#10;                }&#10;                .onFailure { e -&gt; _uiState.value = LoginUiState.Error(e.message ?: &quot;登录失败&quot;) }&#10;        }&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;/**&#10; * 认证仓库契约接口，定义登录、注册与登出操作。&#10; * 实现类负责向后端发起 HTTP 请求并管理本地会话持久化。&#10; */&#10;interface AuthRepository {&#10;    /** 用户登录：发送凭证至后端，成功返回 Unit，失败携带异常信息 */&#10;    suspend fun login(email: String, password: String): Result&lt;Unit&gt;&#10;    /** 用户注册：创建新账户并返回认证结果 */&#10;    suspend fun register(email: String, password: String, nickname: String): Result&lt;Unit&gt;&#10;    /** 登出操作：通知服务端并清除本地会话状态 */&#10;    suspend fun logout()&#10;}&#10;```&#10;&#10;```kotlin&#10;@Singleton&#10;class AuthRepositoryImpl @Inject constructor(&#10;    private val authApiService: AuthApiService,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) : AuthRepository {&#10;&#10;    /**&#10;     * 执行远端登录请求并持久化会话信息。&#10;     * 核心数据流：构造请求 → 发出 HTTP 调用 → 校验响应（HTTP 状态码 + 业务码 + 令牌非空）&#10;     * → 依次写入 authToken / refreshToken / userId / userEmail → 返回 Result。&#10;     * @param email    用户邮箱（内部 trim 处理）&#10;     * @param password 明文密码&#10;     * @return Result&lt;Unit&gt; 成功时携带 Unit，失败时携带服务端或网络异常&#10;     */&#10;    override suspend fun login(email: String, password: String): Result&lt;Unit&gt; {&#10;        return try {&#10;            val response = authApiService.login(&#10;                LoginRequestDto(&#10;                    email = email.trim(),&#10;                    password = password,&#10;                    deviceId = userPreferencesManager.getOrCreateDeviceId()  // 设备标识用于多端管理&#10;                )&#10;            )&#10;            val body = response.body()&#10;            // 三重校验：HTTP 成功 + 业务码 200 + 令牌非空&#10;            if (response.isSuccessful &amp;&amp; body?.code == 200 &amp;&amp; !body.data?.token.isNullOrBlank()) {&#10;                val data = requireNotNull(body.data)&#10;                // ↓ 持久化认证令牌链，供后续请求鉴权使用&#10;                userPreferencesManager.saveAuthToken(data.token)&#10;                if (!data.refreshToken.isNullOrBlank()) userPreferencesManager.saveRefreshToken(data.refreshToken)&#10;                data.user?.id?.let { userPreferencesManager.saveBackendUserId(it.toString()) }&#10;                data.user?.email?.let { userPreferencesManager.saveBackendUserEmail(it) }&#10;                Result.success(Unit)&#10;            } else {&#10;                // 业务层错误：提取服务端消息或 HTTP 状态描述&#10;                Result.failure(Exception(body?.message ?: response.message() ?: &quot;登录失败&quot;))&#10;            }&#10;        } catch (e: Exception) {&#10;            // 网络异常或 JSON 序列化失败&#10;            Result.failure(Exception(e.message ?: &quot;登录失败&quot;, e))&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 执行登出操作。&#10;     * 先尝试以 refreshToken 通知服务端注销会话；无论服务端是否响应，&#10;     * 均在 finally 块中清除本地所有会话数据，确保客户端状态一致性。&#10;     */&#10;    override suspend fun logout() {&#10;        try {&#10;            val refreshToken = userPreferencesManager.getRefreshToken()&#10;            if (refreshToken.isNotBlank()) {&#10;                authApiService.logout(LogoutRequestDto(refreshToken = refreshToken))  // 通知服务端&#10;            }&#10;        } finally {&#10;            // 无论网络请求是否成功，强制清除本地会话，防止残留令牌&#10;            userPreferencesManager.clearBackendSession()&#10;        }&#10;    }&#10;}&#10;```&#10;&#10;上述实现表明，登录模块通过输入校验、异步认证与本地会话保存的分层处理，既保证了界面交互的及时性，也让状态传播的边界更加清楚。&#10;&#10;5.1.2 日常记账模块实现&#10;&#10;日常记账模块主要实现收支记录的创建、修改与删除，并向界面提供聚合后的流水数据用于统计与展示。该模块采用离线优先策略，所有写操作优先落库并标记为待同步，待网络可用时再由同步管理器统一处理。UI 层通过 `TransactionViewModel` 暴露以日期或账户为粒度的 Flow，从而保持界面数据的实时更新。&#10;&#10;从架构上看，交易数据遵循领域模型与本地实体映射的约定，DAO 负责流式查询，RepositoryImpl 承担本地优先读写与同步标记，UseCase 层则封装分类汇总、预算预警等业务规则。ViewModel 将交易、账户与分类等数据源组合为 UIState，并通过事件流完成一次性消息分发。&#10;&#10;在 ViewModel 层中，`TransactionViewModel` 负责交易加载、同步状态监听和用户操作入口，核心代码如上所示。Repository 层中，`TransactionRepositoryImpl` 负责本地优先的增删改查与待同步标记，核心流程是先完成本地写入，再根据网络状态决定是否触发后台同步。&#10;&#10;```kotlin&#10;// 说明：该 ViewModel 负责加载当天交易、监听同步状态以及处理新增、删除等入口，核心流程围绕本地数据加载与同步触发展开。&#10;@HiltViewModel&#10;class TransactionViewModel @Inject constructor(&#10;    private val transactionUseCases: TransactionUseCases,&#10;    private val syncManager: SyncManager,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val realtimeAssetCalculator: RealtimeAssetCalculator,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) : BaseViewModel&lt;TransactionUiState, TransactionUiEvent&gt;() {&#10;    override fun initialState() = TransactionUiState()&#10;&#10;    init {&#10;        loadData()              // 加载当日交易与账户数据&#10;        initDefaultAccounts()   // 若账户表为空则初始化默认账户&#10;        observeSyncState()      // 监听同步状态与待同步数量&#10;        observeRealtimeAsset()  // 订阅实时资产流，驱动界面分钟级刷新&#10;    }&#10;&#10;    /**&#10;     * 添加一笔交易记录。&#10;     * 数据流：调用 UseCase 写入（内部标记待同步）→ 关闭对话框 → 发送成功事件&#10;     * → 根据网络状态决定是否立即触发全量同步。&#10;     * @param amount    交易金额&#10;     * @param type      收支类型（收入/支出）&#10;     * @param category  分类标签&#10;     * @param accountId 所属账户 ID&#10;     * @param note      备注（可选）&#10;     * @param date      交易时间戳，默认为当前时刻&#10;     */&#10;    fun addTransaction(&#10;        amount: Double,&#10;        type: TransactionType,&#10;        category: String,&#10;        accountId: Long,&#10;        note: String = &quot;&quot;,&#10;        date: Long = System.currentTimeMillis()&#10;    ) {&#10;        viewModelScope.launch {&#10;            transactionUseCases.addTransaction(&#10;                amount = amount,&#10;                type = type,&#10;                category = category,&#10;                accountId = accountId,&#10;                note = note,&#10;                date = date&#10;            )&#10;            hideAddDialog()&#10;            sendEvent(TransactionUiEvent.TransactionAdded)&#10;            // 根据网络状态提供差异化提示&#10;            sendEvent(TransactionUiEvent.ShowMessage(if (networkMonitor.isNetworkAvailable()) &quot;记账成功&quot; else &quot;记账成功（离线模式，稍后自动同步）&quot;))&#10;            if (networkMonitor.isNetworkAvailable()) syncManager.syncAll()  // 在线时立即同步&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 删除指定交易记录，成功后触发同步。&#10;     * @param transaction 待删除的交易领域对象&#10;     */&#10;    fun deleteTransaction(transaction: Transaction) {&#10;        viewModelScope.launch {&#10;            transactionUseCases.deleteTransaction(transaction)&#10;            sendEvent(TransactionUiEvent.TransactionDeleted)&#10;            sendEvent(TransactionUiEvent.ShowMessage(&quot;删除成功&quot;))&#10;            if (networkMonitor.isNetworkAvailable()) syncManager.syncAll()&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 订阅 RealtimeAssetCalculator 输出的实时资产流，&#10;     * 将 currentAsset / incomePerMinute / expensePerMinute / netChangePerMinute 映射到 UIState。&#10;     */&#10;    private fun observeRealtimeAsset() {&#10;        realtimeAssetCalculator.realtimeAsset.onEach { assetData -&gt;&#10;            updateState {&#10;                copy(&#10;                    realtimeAsset = assetData.currentAsset,&#10;                    incomePerMinute = assetData.incomePerMinute,&#10;                    expensePerMinute = assetData.expensePerMinute,&#10;                    netChangePerMinute = assetData.netChangePerMinute&#10;                )&#10;            }&#10;        }.launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 监听 SyncManager 的同步状态与待同步计数。&#10;     * 同步成功时向用户展示已同步条数，失败时展示错误信息。&#10;     */&#10;    private fun observeSyncState() {&#10;        syncManager.syncState.onEach { state -&gt;&#10;            updateState { copy(isSyncing = state is SyncState.Syncing, isOffline = !networkMonitor.isNetworkAvailable()) }&#10;            when (state) {&#10;                is SyncState.Success -&gt; if (state.syncedCount &gt; 0) sendEvent(TransactionUiEvent.ShowMessage(&quot;同步完成，共同步 ${state.syncedCount} 条记录&quot;))&#10;                is SyncState.Error -&gt; sendEvent(TransactionUiEvent.ShowMessage(&quot;同步失败: ${state.message}&quot;))&#10;                else -&gt; {}&#10;            }&#10;        }.launchIn(viewModelScope)&#10;        syncManager.pendingSyncCount.onEach { count -&gt; updateState { copy(pendingSyncCount = count) } }.launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 若本地账户表为空，则创建预设默认账户（如现金、银行卡等），&#10;     * 保证首次使用时的记账体验。&#10;     */&#10;    private fun initDefaultAccounts() {&#10;        viewModelScope.launch { transactionUseCases.initializeDefaultAccountsIfEmpty() }&#10;    }&#10;&#10;    /**&#10;     * 加载当日交易数据，取今日起止时间戳，通过 UseCase 层获取聚合结果后填充 UIState。&#10;     * 数据源通过 Flow 持续推送，界面随本地数据变更自动刷新。&#10;     */&#10;    private fun loadData() {&#10;        updateState { copy(loadingState = LoadingState.LOADING) }&#10;        val todayStart = DateTimeUtil.getTodayStartTimestamp()&#10;        val todayEnd = DateTimeUtil.getTodayEndTimestamp()&#10;        transactionUseCases.loadTransactionsByDateRange(todayStart, todayEnd).onEach { result -&gt;&#10;            updateState {&#10;                copy(&#10;                    transactions = result.transactions,&#10;                    dailyTransactions = result.dailyTransactions,&#10;                    accounts = result.accounts,&#10;                    todayIncome = result.todayIncome,&#10;                    todayExpense = result.todayExpense,&#10;                    totalBalance = result.totalBalance,&#10;                    loadingState = LoadingState.SUCCESS&#10;                )&#10;            }&#10;        }.launchIn(viewModelScope)&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该仓库实现负责将交易记录写入本地数据库并在网络可用时触发同步，逻辑重点是本地优先与同步状态维护。&#10;@Singleton&#10;class TransactionRepositoryImpl @Inject constructor(&#10;    private val transactionDao: TransactionDao,&#10;    private val expenseApiService: ExpenseApiService,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val syncManager: SyncManager&#10;) : TransactionRepository {&#10;    // 非核心查询方法省略，核心写入与同步流程完整展示&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 新增交易记录：领域对象转换为实体并标记 PENDING_UPLOAD，先写入本地保证离线可用，&#10;     * 随后在网络可用时触发后台同步。&#10;     * @param transaction 交易领域对象&#10;     * @return 本地生成的记录 ID&#10;     */&#10;    override suspend fun addTransaction(transaction: Transaction): Long {&#10;        // 先写本地并标记待上传，保证离线可用&#10;        val entity = toEntity(transaction).copy(&#10;            syncStatus = SyncStatus.PENDING_UPLOAD,&#10;            updatedAt = System.currentTimeMillis()&#10;        )&#10;        val localId = transactionDao.insert(entity)&#10;&#10;        // 网络可用时立即尝试同步，减少待同步积压&#10;        trySync()&#10;        return localId&#10;    }&#10;&#10;    /**&#10;     * 更新交易记录：保留原有 serverId，将同步状态重置为 PENDING_UPLOAD，&#10;     * 确保后续同步流程能将本地修改上传至后端。&#10;     * @param transaction 包含新值的交易领域对象&#10;     */&#10;    override suspend fun updateTransaction(transaction: Transaction) {&#10;        val existingEntity = transactionDao.getById(transaction.id)&#10;        // 更新后统一回到待上传状态，由同步管理器处理与后端的一致性&#10;        val entity = toEntity(transaction).copy(&#10;            serverId = existingEntity?.serverId,&#10;            syncStatus = SyncStatus.PENDING_UPLOAD,&#10;            updatedAt = System.currentTimeMillis()&#10;        )&#10;        transactionDao.update(entity)&#10;        trySync()&#10;    }&#10;&#10;    /**&#10;     * 删除交易记录：区分&quot;已同步到后端&quot;与&quot;仅存在于本地&quot;两种情况。&#10;     * 已同步的记录执行软删除标记，待后端确认后再物理删除；&#10;     * 未同步的记录直接物理删除，避免产生孤儿同步请求。&#10;     * @param transaction 待删除的交易领域对象&#10;     */&#10;    override suspend fun deleteTransaction(transaction: Transaction) {&#10;        val entity = transactionDao.getById(transaction.id)&#10;        if (entity != null) {&#10;            if (entity.serverId != null) {&#10;                // 已上云数据仅做软删除标记，待后端删除成功后再清理本地&#10;                transactionDao.markAsDeleted(transaction.id)&#10;                trySync()&#10;            } else {&#10;                // 本地未上云数据可直接删除&#10;                transactionDao.delete(entity)&#10;            }&#10;        }&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 网络可用时触发交易同步，作为各写入方法的后置钩子，&#10;     * 减少待同步数据积压并降低跨端冲突概率。&#10;     */&#10;    private suspend fun trySync() {&#10;        if (networkMonitor.isNetworkAvailable()) {&#10;            syncManager.syncTransactions()&#10;        }&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;}&#10;```&#10;&#10;上述实现体现了本地优先的数据写入策略，也说明了通过观察流式数据来更新界面的常见做法。&#10;&#10;5.1.3 固定收支模块实现&#10;&#10;固定收支模块旨在管理周期性收入与支出，并支撑“实时资产引擎”的分钟级动态演进机制。在此场景中，任何长周期的固定收支（如“每日收支”、“每月工资”）都需要折算为其在每分钟内的细粒度累计额，以便实时平滑地反映在总资产变化上。&#10;&#10;**长线演进与浮点数累加误差挑战**&#10;基于频率计算每分钟收支时，常规方案会将“周期总金额”除以“周期总分钟数”（如 1000元 / 1440分钟）。然而，由此衍生出的无限循环小数若是通过“系统轮询定时累加”的方式长线运行，将不可避免地引发 IEEE 754 浮点数截断与精度丢失。随着时间推移，该类微弱误差的不断正向叠加会导致资产账本产生明显错位。&#10;&#10;**差值补偿与周期剥离法（优化策略）**&#10;为了从根本上规避该问题，系统在架构设计层面摒弃了粗暴的“金额累加法”，转而采用**基于时间标度的整除补偿器**策略：&#10;1. **时间标尺唯一化**：系统在定时轮询中仅累加无精度损失的“整型分钟增量”，不对单步金额进行处理。&#10;2. **完整周期剥离**：通过对总累计时长进行整除操作，剥离出“完整的经历周期数（Complete Cycles）”，这部分累计金额直接用 `完整周期数 × 设定金额` 计算，实现“零小数、零妥协”的精准叠加。&#10;3. **零头按比例折算与自动清零**：仅当剔除全部完整周期后，剩下的“零头分钟数（Remaining Minutes）”才会被用作比例折算。由于剩余零头永远小于单个周期，这意味着无论计算运行多少天、多少年，其浮度误差被严格封印在不超过“1个周期”内的微小范围内，并随着新一轮周期的形成而被定期、自动地清零。&#10;&#10;该核心思路分别体现在领域模型内的高内聚金额换算函数，以及外界系统的调用映射上。以下为适用于论述展示的核心代码截取：&#10;&#10;```kotlin&#10;// 截取自 domain/model/FixedIncome.kt -&gt; FixedIncomeFrequency&#10;/**&#10; * 根据累计生效的分钟数（长整型）安全计算累计金额&#10; * 核心在于【周期剥离】，解决长线浮点数累积产生的精度溢出或丢失&#10; */&#10;fun calculateAccumulatedAmount(amount: Double, accumulatedMinutes: Long): Double {&#10;    val minutesPerCycle = getMinutesPerCycle() &#10;    &#10;    // 1. 剥离出已跨越的完整“足额”周期，直接进行乘法放大避免循环小数&#10;    val completeCycles = accumulatedMinutes / minutesPerCycle &#10;    // 2. 取模运算获取当前尚未走完的“零头分钟数”&#10;    val remainingMinutes = accumulatedMinutes % minutesPerCycle &#10;&#10;    // 核心组装：绝对精准的(完整周期额度) + 同比折算的(当期零头额度)&#10;    return (completeCycles * amount) + (remainingMinutes.toDouble() / minutesPerCycle * amount)&#10;}&#10;```&#10;&#10;```kotlin&#10;// 截取自 domain/usecase/RealtimeAssetCalculator.kt&#10;/**&#10; * 为指定固定收支条目计算自上次记录以来的累计&#10; * 设计要点：引擎循环中剥离金额敏感操作，仅作时间整型演进，交由底层映射最终金额&#10; */&#10;private suspend fun updateFixedIncomeAccumulation(&#10;    fixedIncome: FixedIncome,&#10;    currentTime: Long&#10;) {&#10;    // 逻辑抽象：计算距上次运算经过了多少有效分钟(effectiveMinutes)...&#10;    &#10;    // Step1: 各次轮询仅累加无误差的【时间整型常量】&#10;    val newAccumulatedMinutes = fixedIncome.accumulatedMinutes + effectiveMinutes&#10;&#10;    // Step2: 将总时间常量投入上文的【周期剥离补偿公式】，直接还原为精确的总资产额&#10;    val newAccumulatedAmount = fixedIncome.frequency.calculateAccumulatedAmount(&#10;        fixedIncome.amount,&#10;        newAccumulatedMinutes&#10;    )&#10;&#10;    // Step3: 持久化覆写，替换上一次的历史状态&#10;    assetRepository.updateFixedIncomeAccumulation(&#10;        id = fixedIncome.id,&#10;        accumulatedMinutes = newAccumulatedMinutes,&#10;        accumulatedAmount = newAccumulatedAmount, // 避免了增量叠加&#10;        lastRecordTime = currentTime&#10;    )&#10;}&#10;```&#10;&#10;上述实现展示了在移动端高频、长周期的财务金融计算背景下，如何通过对时间维度与货币维度的合理切分重构，以算子融合的方式彻底化解微观计息带来的累加误差隐患漏洞。该模块设计高度符合“单一事实来源（Single Source of Truth）”原则，具有极高的工业参考价值与容错性。&#10;&#10;&#10;&#10;5.1.4 投资管理模块实现&#10;&#10;投资管理模块主要实现投资条目的记录、组合汇总、外部行情拉取与周期性市值刷新。架构上，投资数据通过 `InvestmentRepository` 暴露给 ViewModel，Repository 负责缓存本地数据，并在需要时向后端或第三方行情接口发起请求。为降低界面等待时间，该模块采用异步刷新策略，先展示本地缓存的市值，再在后台完成行情更新与结果回写。&#10;&#10;从实现层面看，需要关注行情请求的限流与去重、不同网络条件下的降级处理，以及价格更新与资产快照之间的一致性。下面的两段代码分别展示 `InvestmentViewModel` 的条目合并与刷新触发逻辑，以及 `InvestmentRepositoryImpl` 的行情拉取与本地更新过程。&#10;&#10;```kotlin&#10;// 说明：该 ViewModel 负责加载投资数据、合并同类条目，并周期性触发股票价格刷新，整体逻辑围绕本地展示和异步行情更新展开。&#10;@HiltViewModel&#10;class InvestmentViewModel @Inject constructor(&#10;    private val investmentDao: InvestmentDao,&#10;    private val investmentRepository: InvestmentRepository&#10;) : BaseViewModel&lt;InvestmentUiState, InvestmentUiEvent&gt;() {&#10;&#10;    private var stockPriceRefreshJob: Job? = null&#10;&#10;    override fun initialState() = InvestmentUiState()&#10;&#10;    init {&#10;        loadInvestments()           // 加载本地投资数据&#10;        startStockPriceRefresh()    // 启动周期性行情刷新&#10;    }&#10;&#10;    /**&#10;     * 从 DAO 加载全部投资条目并进行聚合。&#10;     * 流程：DAO Flow → 实体映射为领域对象 → 同品类合并 → 计算总投入/总市值/总盈亏 → 写入 UIState。&#10;     */&#10;    private fun loadInvestments() {&#10;        updateState { copy(loadingState = LoadingState.LOADING) }&#10;&#10;        investmentDao.getAllInvestments()&#10;            .onEach { entities -&gt;&#10;                val investments = entities.map { toDomainModel(it) }&#10;&#10;                val mergedInvestments = mergeInvestments(investments)&#10;&#10;                val totalInvestment = mergedInvestments.sumOf { it.investment }&#10;                val totalCurrentValue = mergedInvestments.sumOf { it.calcCurrentValue() }&#10;                val totalProfitLoss = totalCurrentValue - totalInvestment&#10;&#10;                updateState {&#10;                    copy(&#10;                        allInvestments = mergedInvestments,&#10;                        filteredInvestments = applyFilter(mergedInvestments, filterType),&#10;                        totalInvestment = totalInvestment,&#10;                        totalCurrentValue = totalCurrentValue,&#10;                        totalProfitLoss = totalProfitLoss,&#10;                        loadingState = LoadingState.SUCCESS&#10;                    )&#10;                }&#10;            }&#10;            .launchIn(viewModelScope)&#10;    }&#10;&#10;    /**&#10;     * 按品类与描述将同名投资条目合并，累加数量、投入成本与当前市值。&#10;     * 分组键为 &quot;category_description&quot;，单条目直接返回，多条目求和后返回副本。&#10;     * @param investments 原始投资列表&#10;     * @return 合并后的投资列表&#10;     */&#10;    private fun mergeInvestments(investments: List&lt;Investment&gt;): List&lt;Investment&gt; {&#10;        return investments&#10;            .groupBy { &quot;${it.category}_${it.description}&quot; }  // 按品类+描述分组&#10;            .map { (_, items) -&gt;&#10;                if (items.size == 1) {&#10;                    items.first()&#10;                } else {&#10;                    val first = items.first()&#10;                    first.copy(&#10;                        quantity = items.sumOf { it.quantity },&#10;                        investment = items.sumOf { it.investment },&#10;                        currentValue = items.sumOf { it.currentValue }&#10;                    )&#10;                }&#10;            }&#10;    }&#10;&#10;    /**&#10;     * 设置筛选类型并立即应用过滤，用于界面切换&quot;全部/股票/其他&quot;视图。&#10;     * @param filterType 筛选枚举值&#10;     */&#10;    fun setFilterType(filterType: InvestmentFilterType) {&#10;        updateState {&#10;            copy(&#10;                filterType = filterType,&#10;                filteredInvestments = applyFilter(allInvestments, filterType)&#10;            )&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 根据筛选类型过滤投资列表。&#10;     * ALL 返回全量，STOCK 仅保留股票类，OTHER 仅保留其他品类。&#10;     */&#10;    private fun applyFilter(list: List&lt;Investment&gt;, filterType: InvestmentFilterType): List&lt;Investment&gt; {&#10;        return when (filterType) {&#10;            InvestmentFilterType.ALL -&gt; list&#10;            InvestmentFilterType.STOCK -&gt; list.filter { it.category == InvestmentCategory.STOCK }&#10;            InvestmentFilterType.OTHER -&gt; list.filter { it.category == InvestmentCategory.OTHER }&#10;        }&#10;    }&#10;&#10;    /** 显示添加对话框，同时清空编辑目标 */&#10;    fun showAddDialog() {&#10;        updateState { copy(showAddDialog = true, editingInvestment = null) }&#10;    }&#10;&#10;    /** 隐藏对话框并发送关闭事件 */&#10;    fun hideAddDialog() {&#10;        updateState { copy(showAddDialog = false, editingInvestment = null) }&#10;        sendEvent(InvestmentUiEvent.DismissDialog)&#10;    }&#10;&#10;    /** 将指定投资条目设为编辑目标并打开对话框 */&#10;    fun editInvestment(investment: Investment) {&#10;        updateState { copy(showAddDialog = true, editingInvestment = investment) }&#10;    }&#10;&#10;    /**&#10;     * 新增投资条目。若已存在同描述同品类的条目，则合并数量与投入；否则插入新行。&#10;     * 股票类投资（STOCK）的 currentValue 由行情刷新驱动，此处仅设投入值；&#10;     * 其他类投资（OTHER）则同步设置 currentValue。&#10;     * @param category     品类（STOCK/OTHER）&#10;     * @param description  描述/名称&#10;     * @param quantity     持有数量&#10;     * @param investment   投入成本&#10;     * @param currentValue 当前市值（非股票类可选指定）&#10;     */&#10;    fun addInvestment(&#10;        category: InvestmentCategory,&#10;        description: String,&#10;        quantity: Double,&#10;        investment: Double,&#10;        currentValue: Double? = null&#10;    ) {&#10;        viewModelScope.launch {&#10;            try {&#10;                val entityCategory = when (category) {&#10;                    InvestmentCategory.STOCK -&gt; EntityCategory.STOCK&#10;                    InvestmentCategory.OTHER -&gt; EntityCategory.OTHER&#10;                }&#10;&#10;                val existing = investmentDao.findByDescriptionAndCategory(description, entityCategory)&#10;&#10;                if (existing != null) {&#10;                    // 合并到已有条目&#10;                    val updated = existing.copy(&#10;                        quantity = existing.quantity + quantity,&#10;                        investment = existing.investment + investment,&#10;                        currentValue = if (category == InvestmentCategory.OTHER &amp;&amp; currentValue != null) {&#10;                            existing.currentValue + currentValue&#10;                        } else {&#10;                            existing.currentValue&#10;                        },&#10;                        updatedAt = System.currentTimeMillis()&#10;                    )&#10;                    investmentDao.update(updated)&#10;                    sendEvent(InvestmentUiEvent.ShowMessage(&quot;已合并到现有条目&quot;))&#10;                } else {&#10;                    // 新建条目&#10;                    val entity = InvestmentEntity(&#10;                        category = entityCategory,&#10;                        description = description,&#10;                        quantity = quantity,&#10;                        investment = investment,&#10;                        currentPrice = 0.0,&#10;                        currentValue = currentValue ?: investment&#10;                    )&#10;                    investmentDao.insert(entity)&#10;                    sendEvent(InvestmentUiEvent.ShowMessage(&quot;添加成功&quot;))&#10;                }&#10;&#10;                hideAddDialog()&#10;                sendEvent(InvestmentUiEvent.InvestmentAdded)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;添加失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 更新投资条目。&#10;     * 股票类保留原有 currentPrice（由行情刷新维护），仅更新描述、数量、成本；&#10;     * 其他类则用传入的 currentValue 直接覆盖。&#10;     * @param id           条目 ID&#10;     * @param category     品类&#10;     * @param description  描述&#10;     * @param quantity     数量&#10;     * @param investment   投入成本&#10;     * @param currentValue 当前市值（非股票类可选）&#10;     */&#10;    fun updateInvestment(&#10;        id: Long,&#10;        category: InvestmentCategory,&#10;        description: String,&#10;        quantity: Double,&#10;        investment: Double,&#10;        currentValue: Double? = null&#10;    ) {&#10;        viewModelScope.launch {&#10;            try {&#10;                val existing = investmentDao.getById(id) ?: return@launch&#10;&#10;                val entityCategory = when (category) {&#10;                    InvestmentCategory.STOCK -&gt; EntityCategory.STOCK&#10;                    InvestmentCategory.OTHER -&gt; EntityCategory.OTHER&#10;                }&#10;&#10;                val updated = existing.copy(&#10;                    category = entityCategory,&#10;                    description = description,&#10;                    quantity = quantity,&#10;                    investment = investment,&#10;                    currentPrice = if (category == InvestmentCategory.STOCK) existing.currentPrice else 0.0,&#10;                    currentValue = if (category == InvestmentCategory.OTHER) {&#10;                        currentValue ?: investment&#10;                    } else {&#10;                        existing.currentValue  // 股票类保持原市值&#10;                    },&#10;                    updatedAt = System.currentTimeMillis()&#10;                )&#10;&#10;                investmentDao.update(updated)&#10;                hideAddDialog()&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;更新成功&quot;))&#10;                sendEvent(InvestmentUiEvent.InvestmentUpdated)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;更新失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 按 ID 删除投资条目，并发送成功与删除事件。&#10;     * @param investment 待删除的投资领域对象&#10;     */&#10;    fun deleteInvestment(investment: Investment) {&#10;        viewModelScope.launch {&#10;            try {&#10;                investmentDao.deleteById(investment.id)&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;删除成功&quot;))&#10;                sendEvent(InvestmentUiEvent.InvestmentDeleted)&#10;            } catch (e: Exception) {&#10;                sendEvent(InvestmentUiEvent.ShowMessage(&quot;删除失败: ${e.message}&quot;))&#10;            }&#10;        }&#10;    }&#10;&#10;    /** InvestmentEntity → Investment 领域模型映射 */&#10;    private fun toDomainModel(entity: InvestmentEntity): Investment {&#10;        return Investment(&#10;            id = entity.id,&#10;            category = when (entity.category) {&#10;                EntityCategory.STOCK -&gt; InvestmentCategory.STOCK&#10;                EntityCategory.OTHER -&gt; InvestmentCategory.OTHER&#10;            },&#10;            description = entity.description,&#10;            quantity = entity.quantity,&#10;            investment = entity.investment,&#10;            currentPrice = entity.currentPrice,&#10;            currentValue = entity.currentValue,&#10;            createdAt = entity.createdAt,&#10;            updatedAt = entity.updatedAt&#10;        )&#10;    }&#10;&#10;    /**&#10;     * 启动周期性股票价格刷新 Job。&#10;     * 先立即执行一次，之后每 60 秒循环刷新，&#10;     * 内部刷新结果（成功/失败）不做 UI 提示，仅做后台静默更新。&#10;     */&#10;    private fun startStockPriceRefresh() {&#10;        stockPriceRefreshJob?.cancel()&#10;        stockPriceRefreshJob = viewModelScope.launch {&#10;            refreshStockPricesInternal()&#10;&#10;            while (isActive) {&#10;                delay(60_000)&#10;                refreshStockPricesInternal()&#10;            }&#10;        }&#10;    }&#10;&#10;    /** 后台静默刷新股票价格，不向用户反馈结果 */&#10;    private suspend fun refreshStockPricesInternal() {&#10;        when (val result = investmentRepository.refreshAllStockPrices()) {&#10;            is Resource.Success -&gt; {}&#10;            is Resource.Error -&gt; {}&#10;            is Resource.Loading -&gt; {}&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 用户手动触发股票价格刷新。&#10;     * 设置刷新中的 UI 状态，完成后根据结果发送成功/失败消息。&#10;     */&#10;    fun refreshStockPrices() {&#10;        viewModelScope.launch {&#10;            updateState { copy(isRefreshing = true) }&#10;            when (val result = investmentRepository.refreshAllStockPrices()) {&#10;                is Resource.Success -&gt; sendEvent(InvestmentUiEvent.ShowMessage(&quot;刷新成功&quot;))&#10;                is Resource.Error -&gt; sendEvent(InvestmentUiEvent.ShowMessage(&quot;刷新失败: ${result.message}&quot;))&#10;                is Resource.Loading -&gt; {}&#10;            }&#10;            updateState { copy(isRefreshing = false) }&#10;        }&#10;    }&#10;&#10;    /** ViewModel 销毁时取消行情刷新 Job，防止协程泄漏 */&#10;    override fun onCleared() {&#10;        super.onCleared()&#10;        stockPriceRefreshJob?.cancel()&#10;    }&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该仓库负责查询股票代码、调用行情接口并把最新价格写回数据库，逻辑重点是先查本地持仓，再批量刷新价格。&#10;@Singleton&#10;class InvestmentRepositoryImpl @Inject constructor(&#10;    private val investmentDao: InvestmentDao,&#10;    private val stockApiService: StockApiService&#10;) : InvestmentRepository {&#10;&#10;    companion object {&#10;        private const val TAG = &quot;InvestmentRepository&quot;&#10;    }&#10;&#10;    // 常规 CRUD 与映射方法省略，保留行情刷新核心流程&#10;    /* logic omitted for brevity */&#10;&#10;    /**&#10;     * 批量刷新全部持仓股票的最新价格。&#10;     * 流程：查询本地持仓代码 → 转换为行情接口所需格式 → 批量请求 → 解析响应 →&#10;     * 按原始代码回写 currentPrice 至数据库。&#10;     * 若持仓为空则直接返回成功；若响应中无有效行情数据则返回 Error。&#10;     * @return Resource&lt;Unit&gt; 成功时携带 Unit，失败时携带错误描述&#10;     */&#10;    override suspend fun refreshAllStockPrices(): Resource&lt;Unit&gt; {&#10;        return try {&#10;            // 读取本地持仓代码，作为后端行情代理的输入&#10;            val stockCodes = investmentDao.getAllStockCodes()&#10;            Log.d(TAG, &quot;获取到的股票代码: $stockCodes&quot;)&#10;&#10;            if (stockCodes.isEmpty()) {&#10;                Log.d(TAG, &quot;没有股票需要刷新价格&quot;)&#10;                return Resource.Success(Unit)&#10;            }&#10;&#10;            // 统一代码格式后批量请求，减少网络往返&#10;            val sinaSymbols = stockCodes.map { convertToSinaSymbol(it) }&#10;            val symbolsParam = sinaSymbols.joinToString(&quot;,&quot;)&#10;            Log.d(TAG, &quot;请求后端股票行情 API, symbols: $symbolsParam&quot;)&#10;&#10;            val response = stockApiService.getBatchQuotes(BatchQuoteRequest(sinaSymbols))&#10;            Log.d(TAG, &quot;API响应码: ${response.code()}, 是否成功: ${response.isSuccessful}&quot;)&#10;&#10;            if (response.isSuccessful &amp;&amp; response.body() != null) {&#10;                val quotes = response.body()!!.data?.quotes.orEmpty()&#10;                Log.d(TAG, &quot;解析到 ${quotes.size} 个股票价格&quot;)&#10;&#10;                if (quotes.isEmpty()) {&#10;                    Log.w(TAG, &quot;没有获取到有效的股票价格数据&quot;)&#10;                    return Resource.Error(&quot;未找到股票数据，请检查股票代码格式&quot;)&#10;                }&#10;&#10;                // 逐个回写数据库，按原始代码匹配确保一致性&#10;                quotes.forEach { result -&gt;&#10;                    Log.d(TAG, &quot;更新股票 ${result.symbol} 价格: ${result.currentPrice ?: 0.0}&quot;)&#10;                    val originalCode = findOriginalCode(stockCodes, result.symbol)&#10;                    if (originalCode != null) {&#10;                        // 按原始代码回写数据库，确保界面列表与本地数据一致&#10;                        investmentDao.updateStockPrice(originalCode, result.currentPrice ?: 0.0)&#10;                    }&#10;                }&#10;&#10;                Resource.Success(Unit)&#10;            } else {&#10;                val errorBody = response.errorBody()?.string()&#10;                Log.e(TAG, &quot;API请求失败: ${response.code()}, 错误信息: $errorBody&quot;)&#10;                Resource.Error(&quot;获取股票价格失败: ${response.code()} - ${response.message()}&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            Log.e(TAG, &quot;刷新股票价格异常&quot;, e)&#10;            Resource.Error(&quot;网络错误: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 将本地股票代码转换为行情接口所需的 Symbol 格式。&#10;     * 规则：&#10;     *   6 位数字且首位为 6 → sh 前缀（沪市）；&#10;     *   6 位数字且首位为 0 或 3 → sz 前缀（深市）；&#10;     *   5 位数字 → hk 前缀（港股）；&#10;     *   已含 SH/SZ/HK/GB_ 前缀 → 转小写保留；&#10;     *   其他 → gb_ 前缀（美股等）。&#10;     * @param code 本地存储的股票代码&#10;     * @return 行情接口格式的 Symbol 字符串&#10;     */&#10;    private fun convertToSinaSymbol(code: String): String {&#10;        val upperCode = code.uppercase().trim()&#10;&#10;        return when {&#10;            upperCode.startsWith(&quot;SH&quot;) || upperCode.startsWith(&quot;SZ&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.startsWith(&quot;HK&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.startsWith(&quot;GB_&quot;) -&gt; upperCode.lowercase()&#10;            upperCode.matches(Regex(&quot;^6\\d{5}$&quot;)) -&gt; &quot;sh$upperCode&quot;&#10;            upperCode.matches(Regex(&quot;^[03]\\d{5}$&quot;)) -&gt; &quot;sz$upperCode&quot;&#10;            upperCode.matches(Regex(&quot;^\\d{5}$&quot;)) -&gt; &quot;hk$upperCode&quot;&#10;            else -&gt; &quot;gb_${upperCode.lowercase()}&quot;&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 反向查找原始代码。行情接口返回的 Symbol 格式与本地存储格式不同，&#10;     * 需将返回的 Symbol 与本地代码逐一比对（均转换为统一格式后比较），&#10;     * 以找到对应原始代码进行数据库回写。&#10;     * @param originalCodes 本地存储的股票代码列表&#10;     * @param sinaSymbol    行情接口返回的 Symbol&#10;     * @return 匹配的原始代码，若未匹配则返回 null&#10;     */&#10;    private fun findOriginalCode(originalCodes: List&lt;String&gt;, sinaSymbol: String): String? {&#10;        val sinaUpper = sinaSymbol.uppercase()&#10;&#10;        for (code in originalCodes) {&#10;            val converted = convertToSinaSymbol(code).uppercase()&#10;            if (converted == sinaUpper) {&#10;                return code&#10;            }&#10;        }&#10;&#10;        return null&#10;    }&#10;&#10;    /* logic omitted for brevity */&#10;}&#10;```&#10;&#10;该模块体现了本地优先展示与异步刷新的实现思路，使投资数据能够在不阻塞界面的前提下保持较高的时效性。&#10;&#10;5.1.5 数据管理模块实现&#10;&#10;数据管理模块主要实现本地持久化结构定义、DAO 接口设计、Repository 实现以及跨端数据同步管理。实现策略以离线优先为核心，所有实体均包含 `serverId`、`syncStatus` 与 `lastSyncAt` 等字段，以便在网络恢复后执行增量同步。Schema 演化采用 Room 的迁移机制，复杂迁移脚本需在构建阶段或迁移工具中提前验证。&#10;&#10;在实现层面，DAO 保持细粒度查询方法并返回 Flow，与 ViewModel 和 UseCase 层自然衔接。RepositoryImpl 遵循本地优先、网络回补的策略，`SyncManager` 则负责消费待同步队列并与后端保持幂等交互。下面的两段代码分别展示 `TransactionDao` 的查询与同步状态接口，以及 `SyncManager` 的交易同步入口。&#10;&#10;```kotlin&#10;// 说明：该 DAO 提供交易查询、增删改和同步状态维护接口，整体逻辑是把本地数据与同步队列分开管理。&#10;@Dao&#10;interface TransactionDao {&#10;    /**&#10;     * 按日期范围查询交易记录，以 Flow 形式返回并按日期降序排列，&#10;     * 供界面层订阅以获取实时更新的流水数据。&#10;     */&#10;    @Query(&quot;SELECT * FROM transactions WHERE date BETWEEN :startDate AND :endDate ORDER BY date DESC&quot;)&#10;    fun getTransactionsByDateRange(startDate: Long, endDate: Long): Flow&lt;List&lt;TransactionEntity&gt;&gt;&#10;&#10;    /**&#10;     * 查询所有待同步交易（默认状态：PENDING_UPLOAD 和 PENDING_DELETE），&#10;     * 由 SyncManager 批量消费。&#10;     */&#10;    @Query(&quot;SELECT * FROM transactions WHERE syncStatus IN (:statuses)&quot;)&#10;    suspend fun getPendingSyncTransactions(statuses: List&lt;SyncStatus&gt; = listOf(SyncStatus.PENDING_UPLOAD, SyncStatus.PENDING_DELETE)): List&lt;TransactionEntity&gt;&#10;&#10;    /**&#10;     * 更新指定交易的同步状态与最后同步时间，&#10;     * 通常在成功同步或冲突解决后调用。&#10;     */&#10;    @Query(&quot;UPDATE transactions SET syncStatus = :status, lastSyncAt = :syncTime WHERE id = :id&quot;)&#10;    suspend fun updateSyncStatus(id: Long, status: SyncStatus, syncTime: Long = System.currentTimeMillis())&#10;}&#10;```&#10;&#10;```kotlin&#10;// 说明：该同步管理器负责统一处理交易与账户的上传、删除和冲突解决，核心逻辑是先检查网络与会话，再批量同步本地待处理数据。&#10;@Singleton&#10;class SyncManager @Inject constructor(&#10;    private val transactionDao: TransactionDao,&#10;    private val accountDao: AccountDao,&#10;    private val syncMetadataDao: SyncMetadataDao,&#10;    private val expenseApiService: ExpenseApiService,&#10;    private val accountApiService: AccountApiService,&#10;    private val networkMonitor: NetworkMonitor,&#10;    private val userPreferencesManager: UserPreferencesManager&#10;) {&#10;    /** 同步状态流，ViewModel 层订阅以展示同步进度 */&#10;    private val _syncState = MutableStateFlow&lt;SyncState&gt;(SyncState.Idle)&#10;    val syncState: StateFlow&lt;SyncState&gt; = _syncState.asStateFlow()&#10;&#10;    /** 待同步条目计数流，供界面展示离线积压数量 */&#10;    private val _pendingSyncCount = MutableStateFlow(0)&#10;    val pendingSyncCount: StateFlow&lt;Int&gt; = _pendingSyncCount.asStateFlow()&#10;&#10;    companion object {&#10;        const val TABLE_TRANSACTIONS = &quot;transactions&quot;&#10;        const val TABLE_ACCOUNTS = &quot;accounts&quot;&#10;    }&#10;&#10;    init {&#10;        observeNetworkAndSync()     // 监听网络恢复，自动触发同步&#10;        updatePendingSyncCount()    // 初始化待同步计数&#10;    }&#10;&#10;    /**&#10;     * 监听网络状态变化。当网络恢复且存在待同步条目且用户已登录时，自动触发全量同步，&#10;     * 实现离线操作后网络恢复时的透明数据上传。&#10;     */&#10;    private fun observeNetworkAndSync() {&#10;        scope.launch {&#10;            networkMonitor.observeNetworkStatus()&#10;                .filter { it == NetworkStatus.AVAILABLE }  // 仅在网络可用时触发&#10;                .collect {&#10;                    if (_pendingSyncCount.value &gt; 0 &amp;&amp; userPreferencesManager.hasBackendSession()) {&#10;                        syncAll()&#10;                    }&#10;                }&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 重新计算待同步条目总数（交易 + 账户），更新 _pendingSyncCount。&#10;     * 在各同步操作完成后调用以反映最新积压状态。&#10;     */&#10;    private fun updatePendingSyncCount() {&#10;        scope.launch {&#10;            val transactionCount = transactionDao.getPendingSyncCount()&#10;            val accountCount = accountDao.getPendingSyncCount()&#10;            _pendingSyncCount.value = transactionCount + accountCount&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 全量同步入口。&#10;     * 前置校验：检查用户登录状态与网络可用性；&#10;     * 执行流程：依次同步交易与账户，累加成功计数，最后更新待同步计数与同步状态。&#10;     * @return SyncResult&lt;Int&gt; 包含成功同步的总条目数或错误信息&#10;     */&#10;    fun syncAll(): SyncResult&lt;Int&gt; {&#10;        if (!userPreferencesManager.hasBackendSession()) {&#10;            return SyncResult.Error(&quot;未登录，数据已保存在本地，登录后将自动同步&quot;)&#10;        }&#10;&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用，数据已保存在本地&quot;)&#10;        }&#10;&#10;        _syncState.value = SyncState.Syncing&#10;        var totalSynced = 0&#10;&#10;        return try {&#10;            // 先同步交易，再同步账户&#10;            val transactionResult = syncTransactions()&#10;            if (transactionResult is SyncResult.Success) {&#10;                totalSynced += transactionResult.data&#10;            }&#10;&#10;            val accountResult = syncAccounts()&#10;            if (accountResult is SyncResult.Success) {&#10;                totalSynced += accountResult.data&#10;            }&#10;&#10;            _syncState.value = SyncState.Success(totalSynced)&#10;            updatePendingSyncCount()&#10;            SyncResult.Success(totalSynced)&#10;        } catch (e: Exception) {&#10;            val errorMessage = &quot;同步失败: ${e.message}&quot;&#10;            _syncState.value = SyncState.Error(errorMessage, e)&#10;            SyncResult.Error(errorMessage, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 同步交易记录：将本地 PENDING_UPLOAD 条目批量上传，处理 PENDING_DELETE 条目，&#10;     * 并应对 HTTP 409 冲突。&#10;     * 流程：查询待同步列表 → 分离上传/删除 → 上传 → 处理响应（200/409/其他） → 删除本地条目。&#10;     * @return SyncResult&lt;Int&gt; 成功时携带已同步条目数&#10;     */&#10;    fun syncTransactions(): SyncResult&lt;Int&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val pendingTransactions = transactionDao.getPendingSyncTransactions()&#10;            if (pendingTransactions.isEmpty()) {&#10;                return SyncResult.Success(0)&#10;            }&#10;&#10;            // 按同步状态分流&#10;            val toUpload = pendingTransactions.filter { it.syncStatus == SyncStatus.PENDING_UPLOAD }&#10;            val toDelete = pendingTransactions.filter { it.syncStatus == SyncStatus.PENDING_DELETE }&#10;&#10;            var syncedCount = 0&#10;&#10;            // ── 上传待同步条目 ──&#10;            if (toUpload.isNotEmpty()) {&#10;                val dtos = toUpload.map { it.toSyncDto() }&#10;                val lastSyncTime = syncMetadataDao.getByTableName(TABLE_TRANSACTIONS)?.lastSyncTimestamp ?: 0&#10;                val request = SyncRequest(dtos, lastSyncTime)&#10;&#10;                val response = expenseApiService.syncTransactions(request)&#10;                if (response.isSuccessful) {&#10;                    val body = response.body()&#10;                    if (body?.code == 200) {&#10;                        val serverTransactions = body.data.orEmpty()&#10;&#10;                        // 将服务端返回的 ID 回写本地，建立映射&#10;                        for (entity in toUpload) {&#10;                            val match = findServerTransactionForLocal(entity, serverTransactions)&#10;                            val serverId = match?.id?.takeIf { it &gt; 0 }&#10;                            if (serverId != null) {&#10;                                transactionDao.updateServerId(entity.id, serverId.toString(), SyncStatus.SYNCED)&#10;                            } else {&#10;                                transactionDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                            }&#10;                        }&#10;                        syncedCount += toUpload.size&#10;                    } else {&#10;                        return SyncResult.Error(&quot;同步交易失败: ${body?.message}&quot;)&#10;                    }&#10;                } else if (response.code() == 409) {&#10;                    // HTTP 409 表示冲突，触发冲突解决流程&#10;                    return handleTransactionConflict(toUpload)&#10;                } else {&#10;                    return SyncResult.Error(&quot;服务器返回错误: ${response.code()}&quot;)&#10;                }&#10;            }&#10;&#10;            // ── 处理待删除条目 ──&#10;            if (toDelete.isNotEmpty()) {&#10;                for (entity in toDelete) {&#10;                    entity.serverId?.toLongOrNull()?.let { serverId -&gt;&#10;                        val response = expenseApiService.deleteTransaction(serverId)&#10;                        if (response.isSuccessful) {&#10;                            transactionDao.delete(entity)&#10;                            syncedCount++&#10;                        }&#10;                    } ?: run {&#10;                        // 无 serverId 则直接删除（本地未上云数据）&#10;                        transactionDao.delete(entity)&#10;                        syncedCount++&#10;                    }&#10;                }&#10;            }&#10;&#10;            updateSyncMetadata(TABLE_TRANSACTIONS)&#10;            SyncResult.Success(syncedCount)&#10;        } catch (e: Exception) {&#10;            updateSyncError(TABLE_TRANSACTIONS, e.message)&#10;            SyncResult.Error(&quot;同步交易记录失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 处理交易同步冲突（HTTP 409）。&#10;     * 拉取服务端最新数据，以 updatedAt 时间戳判断冲突方向：&#10;     * 若服务端版本更新或本地处于 CONFLICT 状态，则用服务端数据覆盖本地；&#10;     * 完成后将本地条目标记为 SYNCED。&#10;     * @param toUpload 触发冲突的本地待上传条目&#10;     * @return SyncResult&lt;Int&gt; 携带成功解决的条目数&#10;     */&#10;    private fun handleTransactionConflict(toUpload: List&lt;TransactionEntity&gt;): SyncResult&lt;Int&gt; {&#10;        return try {&#10;            val now = System.currentTimeMillis()&#10;            val response = expenseApiService.getTransactions(0, now)&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverTransactions = response.body()?.data ?: emptyList()&#10;                for (serverTx in serverTransactions) {&#10;                    serverTx.id?.let { serverId -&gt;&#10;                        val localEntity = transactionDao.getByServerId(serverId.toString())&#10;                        if (localEntity != null) {&#10;                            // 冲突解决策略：以服务端时间戳为准，更新本地版本&#10;                            if (serverTx.updatedAt != null &amp;&amp;&#10;                                (localEntity.updatedAt &lt; serverTx.updatedAt || localEntity.syncStatus == SyncStatus.CONFLICT)&#10;                            ) {&#10;                                transactionDao.update(serverTx.toEntity().copy(id = localEntity.id))&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;                for (entity in toUpload) {&#10;                    transactionDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                }&#10;                updateSyncMetadata(TABLE_TRANSACTIONS)&#10;                SyncResult.Success(toUpload.size)&#10;            } else {&#10;                SyncResult.Error(&quot;冲突处理失败&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;冲突处理失败: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 同步账户数据。逻辑与 syncTransactions 对称：&#10;     * 上传 PENDING_UPLOAD 条目 → 处理 409 冲突 → 删除 PENDING_DELETE 条目。&#10;     * @return SyncResult&lt;Int&gt; 成功时携带已同步条目数&#10;     */&#10;    fun syncAccounts(): SyncResult&lt;Int&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val pendingAccounts = accountDao.getPendingSyncAccounts()&#10;            if (pendingAccounts.isEmpty()) {&#10;                return SyncResult.Success(0)&#10;            }&#10;&#10;            val toUpload = pendingAccounts.filter { it.syncStatus == SyncStatus.PENDING_UPLOAD }&#10;            val toDelete = pendingAccounts.filter { it.syncStatus == SyncStatus.PENDING_DELETE }&#10;&#10;            var syncedCount = 0&#10;&#10;            if (toUpload.isNotEmpty()) {&#10;                val dtos = toUpload.map { it.toSyncDto() }&#10;                val lastSyncTime = syncMetadataDao.getByTableName(TABLE_ACCOUNTS)?.lastSyncTimestamp ?: 0&#10;                val request = AccountSyncRequest(dtos, lastSyncTime)&#10;&#10;                val response = accountApiService.syncAccounts(request)&#10;                if (response.isSuccessful) {&#10;                    val body = response.body()&#10;                    if (body?.code == 200) {&#10;                        val serverAccounts = body.data ?: emptyList()&#10;&#10;                        for (entity in toUpload) {&#10;                            val match = findServerAccountForLocal(entity, serverAccounts)&#10;                            val serverId = match?.id?.takeIf { it &gt; 0 }&#10;                            if (serverId != null) {&#10;                                accountDao.updateServerId(entity.id, serverId.toString(), SyncStatus.SYNCED)&#10;                            } else {&#10;                                accountDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                            }&#10;                        }&#10;                        syncedCount += toUpload.size&#10;                    } else {&#10;                        return SyncResult.Error(&quot;同步账户失败: ${body?.message}&quot;)&#10;                    }&#10;                } else if (response.code() == 409) {&#10;                    return handleAccountConflict(toUpload)&#10;                } else {&#10;                    return SyncResult.Error(&quot;服务器返回错误: ${response.code()}&quot;)&#10;                }&#10;            }&#10;&#10;            if (toDelete.isNotEmpty()) {&#10;                for (entity in toDelete) {&#10;                    entity.serverId?.toLongOrNull()?.let { serverId -&gt;&#10;                        val response = accountApiService.deleteAccount(serverId)&#10;                        if (response.isSuccessful) {&#10;                            accountDao.delete(entity)&#10;                            syncedCount++&#10;                        }&#10;                    } ?: run {&#10;                        accountDao.delete(entity)&#10;                        syncedCount++&#10;                    }&#10;                }&#10;            }&#10;&#10;            updateSyncMetadata(TABLE_ACCOUNTS)&#10;            SyncResult.Success(syncedCount)&#10;        } catch (e: Exception) {&#10;            updateSyncError(TABLE_ACCOUNTS, e.message)&#10;            SyncResult.Error(&quot;同步账户数据失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 处理账户同步冲突。&#10;     * 与服务端版本比较 updatedAt，若服务端版本更新则覆盖本地数据。&#10;     * @param toUpload 触发冲突的本地待上传账户列表&#10;     * @return SyncResult&lt;Int&gt; 携带成功解决的条目数&#10;     */&#10;    private fun handleAccountConflict(toUpload: List&lt;AccountEntity&gt;): SyncResult&lt;Int&gt; {&#10;        return try {&#10;            val response = accountApiService.getAccounts()&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverAccounts = response.body()?.data ?: emptyList()&#10;                for (serverAcct in serverAccounts) {&#10;                    serverAcct.id?.let { serverId -&gt;&#10;                        val localEntity = accountDao.getByServerId(serverId.toString())&#10;                        if (localEntity != null) {&#10;                            if (serverAcct.updatedAt != null &amp;&amp; localEntity.updatedAt &lt; serverAcct.updatedAt) {&#10;                                accountDao.update(serverAcct.toEntity().copy(id = localEntity.id))&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;                for (entity in toUpload) {&#10;                    accountDao.updateSyncStatus(entity.id, SyncStatus.SYNCED)&#10;                }&#10;                updateSyncMetadata(TABLE_ACCOUNTS)&#10;                SyncResult.Success(toUpload.size)&#10;            } else {&#10;                SyncResult.Error(&quot;账户冲突处理失败&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;账户冲突处理失败: ${e.message}&quot;)&#10;        }&#10;    }&#10;&#10;    /**&#10;     * 从服务端拉取指定时间范围内的交易记录并合并到本地。&#10;     * 对于已存在于本地的记录（以 serverId 匹配），若非本地待上传状态则更新；&#10;     * 未匹配到的记录直接插入本地数据库。&#10;     * @param startDate 查询起始时间戳&#10;     * @param endDate   查询结束时间戳&#10;     * @return SyncResult&lt;List&lt;TransactionEntity&gt;&gt; 携带拉取的实体列表&#10;     */&#10;    fun pullTransactionsFromServer(startDate: Long, endDate: Long): SyncResult&lt;List&lt;TransactionEntity&gt;&gt; {&#10;        if (!networkMonitor.isNetworkAvailable()) {&#10;            return SyncResult.Error(&quot;网络不可用&quot;)&#10;        }&#10;&#10;        return try {&#10;            val response = expenseApiService.getTransactions(startDate, endDate)&#10;            if (response.isSuccessful &amp;&amp; response.body()?.code == 200) {&#10;                val serverTransactions = response.body()?.data ?: emptyList()&#10;                val entities = serverTransactions.map { it.toEntity() }&#10;&#10;                for (entity in entities) {&#10;                    val sid = entity.serverId&#10;                    if (sid != null) {&#10;                        val localEntity = transactionDao.getByServerId(sid)&#10;                        if (localEntity != null) {&#10;                            // 保护本地待上传数据不被覆盖&#10;                            if (localEntity.syncStatus != SyncStatus.PENDING_UPLOAD) {&#10;                                transactionDao.update(entity.copy(id = localEntity.id))&#10;                            }&#10;                        } else {&#10;                            transactionDao.insert(entity)&#10;                        }&#10;                    }&#10;                }&#10;&#10;                SyncResult.Success(entities)&#10;            } else {&#10;                SyncResult.Error(&quot;服务器返回错误: ${response.body()?.message}&quot;)&#10;            }&#10;        } catch (e: Exception) {&#10;            SyncResult.Error(&quot;拉取数据失败: ${e.message}&quot;, e)&#10;        }&#10;    }&#10;&#10;    /** 更新指定表的同步元数据时间戳，记录最后一次成功同步时间 */&#10;    private fun updateSyncMetadata(tableName: String) { /* omitted in doc excerpt */ }&#10;    /** 记录指定表同步过程中的错误信息 */&#10;    private fun updateSyncError(tableName: String, error: String?) { /* omitted in doc excerpt */ }&#10;&#10;    /** 是否存在待同步数据 */&#10;    fun needsSync(): Boolean {&#10;        return _pendingSyncCount.value &gt; 0&#10;    }&#10;&#10;    /** 手动刷新待同步计数 */&#10;    fun refreshPendingCount() {&#10;        updatePendingSyncCount()&#10;    }&#10;&#10;    /**&#10;     * 在服务端返回列表中匹配本地交易实体。&#10;     * 匹配策略：选取 createdAt 时间差最小的服务端记录。&#10;     */&#10;    private fun findServerTransactionForLocal(local: TransactionEntity, serverList: List&lt;TransactionDto&gt;): TransactionDto? { return serverList.minByOrNull { kotlin.math.abs((it.createdAt ?: it.date) - local.createdAt) }&#10;    }&#10;&#10;    /**&#10;     * 在服务端返回列表中匹配本地账户实体，&#10;     * 以 createdAt 最小绝对差作为匹配依据。&#10;     */&#10;    private fun findServerAccountForLocal(local: AccountEntity, serverList: List&lt;AccountDto&gt;): AccountDto? { return serverList.minByOrNull { kotlin.math.abs((it.createdAt ?: 0L) - local.createdAt) }&#10;    }&#10;&#10;    /** TransactionEntity → TransactionDto 映射，用于上传同步 */&#10;    private fun toSyncDto(entity: TransactionEntity): TransactionDto { return TransactionDto(id = entity.serverId?.toLongOrNull() ?: 0L, amount = entity.amount, type = entity.type.name, category = entity.category, note = entity.note, date = entity.date, accountId = entity.accountId.takeIf { it &gt; 0 }, createdAt = entity.createdAt, updatedAt = entity.updatedAt, deletedAt = null) }&#10;&#10;    /** TransactionDto → TransactionEntity 映射，用于服务端数据拉取后的本地转换 */&#10;    private fun toEntity(dto: TransactionDto): TransactionEntity { return TransactionEntity(serverId = dto.id?.takeIf { it &gt; 0 }?.toString(), amount = dto.amount, type = try { TransactionType.valueOf(dto.type) } catch (_: Exception) { TransactionType.EXPENSE }, category = dto.category, accountId = dto.accountId ?: 0, note = dto.note, date = dto.date, createdAt = dto.createdAt ?: dto.date, updatedAt = dto.updatedAt ?: System.currentTimeMillis(), syncStatus = SyncStatus.SYNCED, lastSyncAt = System.currentTimeMillis()) }&#10;&#10;    /** AccountEntity → AccountDto 映射 */&#10;    private fun toSyncDto(entity: AccountEntity): AccountDto { return AccountDto(id = entity.serverId?.toLongOrNull() ?: 0L, name = entity.name, icon = entity.icon, balance = entity.balance, isDefault = entity.isDefault, sortOrder = entity.sortOrder, createdAt = entity.createdAt, updatedAt = entity.updatedAt) }&#10;&#10;    /** AccountDto → AccountEntity 映射 */&#10;    private fun toEntity(dto: AccountDto): AccountEntity { return AccountEntity(serverId = dto.id?.takeIf { it &gt; 0 }?.toString(), name = dto.name, icon = dto.icon, balance = dto.balance, isDefault = dto.isDefault, sortOrder = dto.sortOrder, createdAt = dto.createdAt ?: System.currentTimeMillis(), updatedAt = dto.updatedAt ?: System.currentTimeMillis(), syncStatus = SyncStatus.SYNCED, lastSyncAt = System.currentTimeMillis()) }&#10;}&#10;```&#10;```&#10;&#10;上述实现保证了数据层在离线场景、并发修改与网络不稳定条件下仍可维持一致性与可恢复性，也为后端的最终合并与冲突处理提供了基础。&#10;&#10;参考与说明&#10;文中示例代码均为摘录性质，目的在于说明各模块的核心职责与实现思路。整体示例遵循仓库中既有的 MVVM 与 Clean Architecture 约定，相关符号与文件路径可参照仓库源码，例如 `app/src/main/java/com/example/funnyexpensetracking/ui/transaction/TransactionViewModel.kt`、`domain/usecase/RealtimeAssetCalculator.kt` 以及 `app/src/main/java/com/example/funnyexpensetracking/config/ApiEnvironmentConfig.kt`。如需进一步扩展为可直接编译的完整类，可继续补充上下文与依赖注入细节。&#10;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>